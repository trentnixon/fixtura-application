# Episode — 2026-04-05

## Context

- Implement the **buttons system** plan (kitchen sink first, then shared component and route lab).
- User requested **`LOG:`** to capture outcomes.

## Work Done

- **`src/components/ui/button.tsx`:** **`rounded-full`**; hover lift (**`hover:-translate-ypx`**, **`hover:shadow-md`**, **`hover:border-border/80`**); **`cursor-pointer`** / **`disabled:cursor-not-allowed`**; **`loading`** + **`loadingText`**, **`fullWidth`**, **`size="compact"`**; split **`asChild`** path so **`Slot`** never receives loading fragments.
- **`src/components/ui/button.test.tsx`:** coverage for brand/accent, compact, fullWidth, loading, **`asChild` + loading** ignored.
- **Kitchen sink `/sandbox/kitchen-sink/buttons`:** sections under **`buttons/_sections/`** (intro hierarchy, variants, brand, sizes, states, icons/a11y, contextual usage, action groups, experimental); form actions document **brand** / **accent** instead of **`default`** for submits.
- **`SubmitButton`** ([`src/components/auth/actions.tsx`](src/components/auth/actions.tsx)):** delegates to **`Button`\*\* **`loading`** API; defaults **`buttonVariant="brand"`**; optional **`fullWidth`** (default true), **`loadingText`**; **`{...props}`** spread first so explicit props win.
- **Route lab:** sign-in / forgot-password / create-organisation use **`SubmitButton`** with explicit **`brand`** or **`accent`** and tailored **`loadingText`**; create-org success **Continue** uses **`Button variant="brand"`**; create-org submit uses **`fullWidth={false}`** beside cancel; dashboard stub link uses **`Button variant="link"`** + **`asChild`**.
- **Metadata (Fixtura Members PDR):** **`src/config/metadata.ts`** (**`appMetadata`**, base URL, robots, icons under **`public/logos/`**); **`src/lib/metadata/buildMetadata.ts`** (**`buildRootMetadata`**, **`buildPageMetadata`**); root **`layout.tsx`** uses **`buildRootMetadata()`** with **`title.template`** `%s | Fixtura Members`.
- **Page metadata:** Public routes (home, sign-in, forgot-password, help, support, session-expired, check-email, auth-error, maintenance) and key members routes (dashboard, select-organisation, account settings) export **`buildPageMetadata(...)`**; sandbox pages left as-is per plan.
- **Icons / logos:** **`metadata.icons`** points to **`/logos/favicon-16x16.png`**, **`/logos/favicon-32x32.png`**, **`/logos/favicon.ico`**, **`/logos/apple-touch-icon.png`**; **`auth/layout`**, **`auth/structure`**, **`(public)/page`** `<img>` use **`/logos/apple-touch-icon.png`**; removed tiny duplicate **`public/`** root favicon/icon placeholders.
- **Typography alignment sweep (semantic adoption):** **`empty-state`** / **`error-state`** use **`TypographyEmptyState*`** and **`TypographyAlert*`**; **`section-cards`** uses **`TypographyMetricValue`** / **`TypographyMetricLabel`** + **`TypographyBodySmall`**; **`chart-area-interactive`** card header uses **`TypographyCardTitle`** / **`TypographyCardDescription`**; **`BasicCard`**, **`ListCard`**, **`MediaCard`**, **`ActionHeaderCard`** wrap titles/descriptions; members **`create-organisation`**, admin **`system`**, **`inspector`**, **`fetch-health`** pages updated similarly; **`kitchen-sink/dialogs`** + **`data-table`** drawer + **`command`** **`CommandDialog`** use **`TypographyDialogTitle`** / **`TypographyDialogDescription`** with Radix **`asChild`**; **`nav-user`**, **`site-header`**, **`breadcrumb`** **`BreadcrumbPage`**, **`PublicFooter`** use nav/body typography primitives.
- **Verification:** Vitest for affected UI + full **`src/components/typography`**; **`tsc --noEmit`** clean.

## Decisions

- (Form primaries in members flows: prefer **`brand`** or **`accent`** over **`default`** (blue); documented in kitchen sink.)

## Results

- Single **`Button`** source of truth for global styling and async state; auth **`SubmitButton`** aligned with **`loading`** / **`aria-busy`** behaviour; route lab reflects the same patterns.
- App-wide SEO/tab metadata is centralized with consistent **`| Fixtura Members`** titles; favicon and touch icons load from **`public/logos/`** (no stray root duplicates).
- Shared UI and several routes now consume **`@/components/typography`** semantic primitives (states, metrics, cards, overlays, nav/footer) instead of ad hoc heading/utility classes alone; Radix dialog/drawer **`asChild`** pattern preserved for accessibility.

## Next

- [ ] Optional: smoke **`/sandbox/kitchen-sink/buttons`** and **`/sandbox/route-lab/public/sign-in`** ( **`state=submitting`** ) with dev sandbox on.
- [ ] Optional: add **`loadingText`** on production **`login-form`** / other auth forms for parity with route lab.
- [ ] Optional: set **`NEXT_PUBLIC_APP_URL`** where deployed; spot-check tab title and favicon on sign-in + dashboard.
- [ ] Optional: smoke empty/error states, section cards, kitchen-sink dialogs, and a data-table drawer row after typography changes.
- [ ] Follow-up (not done): bake typography into **`src/components/ui/card.tsx`** primitives; **`data-table`** / **`ui/table`** column text → **`TypographyTable*`** where repetition warrants it; refresh **`src/components/typography/readMe.md`** for new modules.

## Links

- `src/components/ui/button.tsx`, `src/components/auth/actions.tsx`
- `src/app/sandbox/kitchen-sink/buttons/`, `src/app/sandbox/route-lab/public/sign-in/page.tsx`, `src/app/sandbox/route-lab/org/create-organisation/page.tsx`, `src/app/sandbox/route-lab/app/dashboard/page.tsx`
- `src/config/metadata.ts`, `src/lib/metadata/buildMetadata.ts`, `src/app/layout.tsx`, `public/logos/`
- `src/components/typography/index.ts`, `src/components/ui/empty-state.tsx`, `src/components/ui/error-state.tsx`, `src/components/section-cards.tsx`, `src/components/data-table.tsx`, `src/components/ui/command.tsx`, `src/app/sandbox/kitchen-sink/dialogs/page.tsx`
