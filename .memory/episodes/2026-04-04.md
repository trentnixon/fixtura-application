# Episode — 2026-04-04

## Context

- Align implementation with [`.comms/19-Dev-Sandbox-Routes.md`](.comms/19-Dev-Sandbox-Routes.md) and iterate on structure and discoverability.
- User requested LOG to capture outcomes.

## Work Done

- Implemented **dev sandbox gate** (`NEXT_PUBLIC_ENABLE_DEV_SANDBOX === "true"`), `DevSandboxGate`, `isDevSandboxEnabled`, route lab pages, fixtures, `RouteLabPage` / `ScenarioSwitch`, and env documentation in [`.comms/archives/ENVIRONMENT-AND-CONFIG-REFERENCE.md`](.comms/archives/ENVIRONMENT-AND-CONFIG-REFERENCE.md).
- Moved **kitchen sink** under the sandbox tree; added **public top bar** link and **route lab sidebar** portal pattern driven by [`src/lib/dev-sandbox-nav.ts`](src/lib/dev-sandbox-nav.ts).
- **Restructured sandbox URLs:** removed **`(sandbox)`** route group; all sandbox UI now lives under **`/sandbox`** with [`src/app/sandbox/layout.tsx`](src/app/sandbox/layout.tsx) as the single env gate. **`/sandbox`** is the **navigation portal**; **`/sandbox/kitchen-sink/*`** and **`/sandbox/route-lab/*`** nest beneath it.
- Updated [`src/lib/config/routes.ts`](src/lib/config/routes.ts): `sandbox`, `kitchenSink`, `routeLab` paths; **`.skills/`** kitchen sink URLs to `/sandbox/kitchen-sink/...`; env reference and example comms (e.g. kitchen sink URL in private shell doc).

## Decisions

- (See `decisions.md` for durable sandbox URL model.)

## Results

- Old top-level **`/kitchen-sink`** and **`/route-lab`** URLs no longer exist; bookmarks must use **`/sandbox/...`**. Production remains gated off when the env flag is unset or not `true`.

## Next

- [ ] Optionally refresh [`.comms/19-Dev-Sandbox-Routes.md`](.comms/19-Dev-Sandbox-Routes.md) to document **`/sandbox`** as the canonical prefix (spec still lists legacy paths in places).
- [ ] Confirm **preview/CI** env includes `NEXT_PUBLIC_ENABLE_DEV_SANDBOX=true` if automated or human checks need the portal.

## Links

- `src/app/sandbox/`, `src/lib/dev-sandbox.ts`, `src/lib/dev-sandbox-nav.ts`, `src/components/dev/`
- `.comms/19-Dev-Sandbox-Routes.md`, `.comms/archives/ENVIRONMENT-AND-CONFIG-REFERENCE.md`

---

## Addendum — Sandbox typography migration

### Context

- User triggered `LOG:` after implementing the sandbox typography plan: all copy under `src/app/sandbox` should use `@/components/typography` per component-usage patterns.

### Work Done

- Migrated **interaction lab** (layout, overview, six placeholder scenario pages): `TypographyH1`–`TypographyH2`, `TypographyMuted`, `TypographyInlineCode`, `TypographyList` with `className` for muted/spaced lists.
- Migrated **route lab** (overview, dashboard, settings, org create/select, public sign-in/forgot-password): headings and body via typography; **card headers** use `TypographyH3` + `TypographyMuted` instead of `CardTitle`/`CardDescription`; destructive field hints via `TypographyMuted` + `text-destructive`.
- Migrated **kitchen sink** (layout, hub `SECTIONS` map, typography/brand-colors/containers/buttons/cards/navigation/toasts/tables/loading/popovers/inputs/forms/dialogs pages).
- **Primitives:** `TypographyList` and `TypographyLarge` now accept optional `className` merged with `cn()`; tests updated in `list.test.tsx` / `large.test.tsx`.
- **Dialogs kitchen-sink:** `DialogTitle` / `DialogDescription` use Radix `asChild` with `TypographyH3` / `TypographyMuted`.
- Fixed **ESLint import/order** (`kitchen-sink/navigation`, `kitchen-sink/inputs`). **`npm run lint -- --max-warnings 0`** passes.

### Results

- No raw `<h1>`–`<h6>` or `<p>` under `src/app/sandbox/**/*.tsx` (verified via search); portal and nested labs visually aligned with shared typography tokens.

### Next

- [ ] Spot-check `/sandbox`, `/sandbox/route-lab`, `/sandbox/interaction-lab`, `/sandbox/kitchen-sink` in browser after deploy.
- [ ] Optional: migrate shared `PageHeader` in `src/components/ui/container.tsx` to typography if product wants that layer consistent too (out of original sandbox-only scope).

### Links

- `src/components/typography/list.tsx`, `large.tsx`, `src/app/sandbox/**`
- `.skills/component-Usage-Patterns.md`

---

## Addendum — Interaction lab scaffold + dev-Sandbox skill

### Context

- Add the third sandbox tool (**interaction lab**) as routing and static placeholders only (no interaction mechanics yet).
- Capture the sandbox system in **`.skills`** for future agents.

### Work Done

- **`ROUTES.interactionLab`** (`/sandbox/interaction-lab`); portal card, **`INTERACTION_LAB_NAV_SECTIONS`**, **`SandboxRouteLabSidebar`** `portalLinkActive`, **`SiteHeader`** label; sandbox portal grid + route lab overview link to interaction lab.
- **`src/app/sandbox/interaction-lab/`**: layout with sidebar, overview page, six nested **static** placeholder routes (planned-coverage bullets only—no mocks, fixtures, or `ScenarioSwitch`).
- New skill [`.skills/dev-Sandbox.md`](.skills/dev-Sandbox.md) and index entry under **Development sandbox (`/sandbox`)** in [`.skills/index.md`](.skills/index.md).

### Results

- Interaction lab is discoverable like kitchen sink and route lab; placeholders document what to implement later.

### Next

- [ ] Optional: align [`.comms/20-Interaction Lab.md`](.comms/20-Interaction%20Lab.md) example paths with **`/sandbox/interaction-lab/...`** where the doc still uses root **`/interaction-lab`**.

### Links

- `src/app/sandbox/interaction-lab/`, `src/lib/dev-sandbox-nav.ts`, `src/lib/config/routes.ts`, `.skills/dev-Sandbox.md`

---

## Addendum — Kitchen sink hub grid (4 columns)

### Context

- User wanted the **Design Reference** landing grid on the kitchen sink hub to show **four cards per row** instead of many narrow columns (previously up to six columns on `xl`).

### Work Done

- Updated [`src/app/sandbox/kitchen-sink/page.tsx`](src/app/sandbox/kitchen-sink/page.tsx) section grid from `md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-6` to **`grid-cols-1 sm:grid-cols-2 lg:grid-cols-4`**.

### Results

- Large viewports show a **4-column** card grid; smaller breakpoints stack to two then one column.

### Next

- [ ] None required; optional browser check of `/sandbox/kitchen-sink`.

### Links

- `src/app/sandbox/kitchen-sink/page.tsx`

---

## Addendum — Sandbox RSC: Lucide icons and client `GridCard`

### Context

- **`GET /sandbox` returned 500** with React errors: non-serializable props to Client Components (`icon={{$$typeof…}}`), and explicit failures for **`Route`**, **`LayoutGrid`**, **`Zap`** (Lucide) passed from Server Components into **`GridCard` / `GridCardIcon`**.

### Work Done

- Extracted icon-bearing grids into **`"use client"`** modules so \*\*`LucideIcon` references never cross the server → client boundary:
  - [`src/app/sandbox/sandbox-portal-grid.tsx`](src/app/sandbox/sandbox-portal-grid.tsx) — portal tile map + **`SANDBOX_TOOL_ICONS`**
  - [`src/app/sandbox/kitchen-sink/kitchen-sink-overview-grid.tsx`](src/app/sandbox/kitchen-sink/kitchen-sink-overview-grid.tsx) — kitchen sink **`SECTIONS`** + grid
- Thin server pages: [`src/app/sandbox/page.tsx`](src/app/sandbox/page.tsx) (keeps **`metadata`** + header) and [`src/app/sandbox/kitchen-sink/page.tsx`](src/app/sandbox/kitchen-sink/page.tsx) re-export the client grids.

### Results

- **`/sandbox`** and **`/sandbox/kitchen-sink`** compile without RSC serialization errors; portal and hub behaviour unchanged aside from module split.

### Next

- [ ] Quick browser smoke test of **`/sandbox`** and **`/sandbox/kitchen-sink`** when convenient.

### Links

- `src/app/sandbox/sandbox-portal-grid.tsx`, `src/app/sandbox/kitchen-sink/kitchen-sink-overview-grid.tsx`, `src/components/ui/grid-card.tsx`

---

## Addendum — `GridCard` semantic `tone` + select-organisation layout

### Context

- Extend the shared grid tile with semantic surface states; tune members **Select organisation** so the create tile reads as secondary and the grid aligns to the start.

### Work Done

- **`GridCard`:** added **`tone`** (`default` | `mute` | `error` | `success` | `warning` | `loading`) composing with **`variant`** (`default` | `reverse`); shared **`GridCardSurfaceContext`** with **`useGridCardTone()`** / **`useGridCardVariant()`**; tile/title/CTA and **`GridCardVisualSlot`** / **`GridCardIcon`** use brand **`--success`** / **`--warning`** and **`destructive`** where applicable; **`loading`** sets pulse, **`aria-busy`**, and **`pointer-events-none`** on the tile.
- **`/select-organisation`:** **Create organisation** uses **`tone="mute"`** with **`variant="reverse"`**; org grid uses **`justify-items-start`**, empty-state row **`justify-start`**, and **`className="mx-0"`** on cards so **`GridCard`**’s default **`mx-auto`** does not re-center tiles in columns.

### Results

- Members can reuse one component for status-like tiles without one-off classes; select-org list and empty state align left consistently.

### Next

- [ ] Optional: add tone samples to **`/sandbox/kitchen-sink/cards`** exploration if designers want a reference matrix.

### Links

- `src/components/ui/grid-card.tsx`, `src/app/(members)/select-organisation/select-organisation-content.tsx`

---

## Addendum — Select organisation: account row vs `accountOrganisationDetails`

### Context

- `/select-organisation` must iterate **`accounts[]`** from GET account/me; **display** (name, logo, sport, PlayHQ href) comes from **`account.accountOrganisationDetails`**, not the top-level account fields. **`account.id`** remains the membership / scoped-route key.

### Work Done

- **`organisationDetailsFromAccountRow`** in [`src/lib/account/account-me-rows.ts`](src/lib/account/account-me-rows.ts): `contentHub?.accountOrganisationDetails ?? accountOrganisationDetails` (hub first for legacy); Vitest coverage in `account-me-rows.test.ts`.
- Types in [`src/types/api/account.ts`](src/types/api/account.ts): **`AccountOrganisationDetails`**, optional fields on **`AccountSummary`** for API rows.
- **`GridCardSelectOrganisation`** in [`src/components/ui/grid-card.tsx`](src/components/ui/grid-card.tsx): non-square tile, optional sport line, Active/Setup lines with green/red dots (same row), optional CTA removed from default; **`GridCardVisualSlot`** supports org **`imageSrc`** / **`imageAlt`** for **`ParentLogo`**; inner layout **`justify-between`** for equal-height flex rows.
- [`src/components/layout/account-switcher.tsx`](src/components/layout/account-switcher.tsx) uses the same resolver for labels.
- [`src/app/(members)/select-organisation/select-organisation-content.tsx`](<src/app/(members)/select-organisation/select-organisation-content.tsx>): **`flex flex-wrap items-stretch`** with **`max-w-56`** wrappers, **`h-full`** on tiles; **`CreateOrganisationGridCard`** accepts optional **`className`** for stretch.

### Results

- Org cards show the correct organisation title, sport, logo, and status; dashboard links stay **`accountScopedRoutes.dashboard(account.id)`**.

### Next

- [ ] Optional: secondary PlayHQ link on the card if product wants it visible.
- [ ] Visual check: **Create organisation** tile next to taller org cards ( **`aspect-square`** vs stretch).

### Links

- `src/lib/account/account-me-rows.ts`, `src/types/api/account.ts`, `src/app/(members)/select-organisation/select-organisation-content.tsx`, `src/components/ui/grid-card.tsx`, `src/components/layout/account-switcher.tsx`

---

## Addendum — Multi-organisation route plan: gateway UX + tests

### Context

- User requested **`LOG:`** after implementing the multi-org implementation review plan: better handling for **logged-out bookmarks**, **unauthorised or missing org URLs**, and **invalid org id**; align **account switcher** with legacy **`account-me-rows`** fallback; tests and **`.comms/18`** doc sync.

### Work Done

- **`src/lib/config/gateway-reasons.ts`:** stable `reason` enums, `selectOrganisationUrlWithReason`, `selectOrgReasonFromApiStatus`, user messages; **`LOGIN_REASON_CONTINUE`** for middleware sign-in redirect.
- **`src/lib/auth/member-route-sign-in.ts`:** `applySignInRedirectQuery` (used by **`middleware.ts`**); Vitest in **`member-route-sign-in.test.ts`**.
- **`LoginForm`:** info banner when **`from`** is a safe scoped return path; middleware sets **`reason=continue`** for safe **`from`**.
- **`OrgAccessBoundary`:** invalid path segment → **`invalid_org`** without Strapi fetch; API **400/403/404** → mapped reasons; **`useAccountOrganisation`** only when segment valid; reset redirect ref on **`accountId`** change.
- **`select-organisation`:** **`select-organisation-content.tsx`** + Suspense **`page.tsx`**; warning banner + dismiss for gateway **`reason`** query.
- **`accountPickerRowsFromMePayload`** in **`account-me-rows.ts`**; **`AccountSwitcher`** uses same rows as selector.
- **`create-organisation`:** placeholder copy points at **`.comms/18`**; still no CMS API.
- **`.comms/18-FIXTURA_MULTI_ORGANISATION_ROUTE_LOGIC.md`:** `/app` vs **`/o/[accountId]`** note; table rows for deep-link + gateway banners.
- **Tests:** **`gateway-reasons.test.ts`**, **`account-me-rows.test.ts`**, **`account-routes.test.ts`**, **`middleware.test.ts`** (matcher); existing **`safe-return-path.test.ts`** unchanged.

### Results

- Users get explicit messaging instead of silent redirects for the three org-access cases; security unchanged (allowlisted **`from`**, enum **`reason`** only).

### Next

- [ ] Manual smoke: bookmark **`/o/...`** while logged out; open org id user cannot access; malformed id.
- [ ] Wire **create organisation** when CMS contract lands.

### Links

- `src/lib/config/gateway-reasons.ts`, `src/lib/auth/member-route-sign-in.ts`, `src/middleware.ts`, `src/components/auth/org-access-boundary.tsx`, `src/components/auth/login-form.tsx`, `src/app/(members)/select-organisation/`, `.comms/18-FIXTURA_MULTI_ORGANISATION_ROUTE_LOGIC.md`

---

## Addendum — `LOG:` (select-organisation milestone)

### Context

- User ran **`LOG:`** to capture the members gateway **`/select-organisation`** work in memory.

### Work Done

- No additional code in this logging step; episode **“Select organisation: account row vs `accountOrganisationDetails`”** above remains the technical record (grid alignment with sandbox route lab, **`GridCardSelectOrganisation`**, **`CreateOrganisationGridCard`**, **`organisationDetailsFromAccountRow`**, **`max-w-5xl`** layout).

### Results

- **`/.memory/state.md`** refreshed to match current priorities.

### Next

- [ ] Wire **create organisation** when CMS contract is ready.
- [ ] Optional smoke: **`/select-organisation`** with multi-account payload after deploy.

### Links

- `src/app/(members)/select-organisation/select-organisation-content.tsx`

---

## Addendum — Members sidebar: Sandbox link, remove Kitchen Sink

### Context

- Align authenticated **members** navigation with the public **`PublicTopBar`** pattern: env-gated **Sandbox** entry to the portal (`/sandbox`), and remove the direct **Kitchen Sink** sidebar link.

### Work Done

- Updated [`src/components/app-sidebar.tsx`](src/components/app-sidebar.tsx): import **`isDevSandboxEnabled`** from **`@/lib/dev-sandbox`**; **`secondary`** nav spreads **`{ title: "Sandbox", url: ROUTES.sandbox, icon: IconLayoutGrid }`** only when the flag is true; removed **`Kitchen Sink`** / **`ROUTES.kitchenSink`** / **`IconChartBar`**.

### Results

- When **`NEXT_PUBLIC_ENABLE_DEV_SANDBOX`** is not **`true`**, neither Sandbox nor Kitchen Sink appears in the members secondary nav; when enabled, **Sandbox** matches public behavior and kitchen sink remains available from the **`/sandbox`** portal.

### Next

- [ ] Optional: smoke members shell with flag on/off after deploy.

### Links

- `src/components/app-sidebar.tsx`, `src/lib/dev-sandbox.ts`, `src/lib/config/routes.ts`

---

## Addendum — Kitchen sink `/cards` grid showcase + client module

### Context

- Expand **`/sandbox/kitchen-sink/cards`** so the grid-card section documents every public surface from **`@/components/ui/grid-card`** (variants, tones, **`GridCardVisualSlot`** presets and emphasis, **`GridCardIcon`**, **`GridCardSelectOrganisation`**).
- **`LOG:`** to record the work.

### Work Done

- Rewrote [`src/app/sandbox/kitchen-sink/cards/grid-card-exploration.tsx`](src/app/sandbox/kitchen-sink/cards/grid-card-exploration.tsx): subsections for visual presets (org initials, logo, add, sandbox, icon), default vs reverse, tone matrices for default and reverse variants, visual-slot emphasis, select-organisation tiles (including create-org and loading), and sandbox-portal-style **`GridCardIcon`** links.
- Set **`"use client"`** on that module so **Lucide** icon references passed to **`GridCardIcon`** do not cross the RSC boundary (same pattern as **`sandbox-portal-grid.tsx`** / **`kitchen-sink-overview-grid.tsx`**).
- Tuned [`src/app/sandbox/kitchen-sink/cards/page.tsx`](src/app/sandbox/kitchen-sink/cards/page.tsx) **PageHeader** copy to mention grid tiles alongside standard cards.

### Results

- Designers and agents have a single curated page for **`GridCard`** behaviour; no runtime serialization errors for **`icon={…}`**.

### Next

- [ ] Optional: browser smoke **`/sandbox/kitchen-sink/cards`** when convenient.

### Links

- `src/app/sandbox/kitchen-sink/cards/grid-card-exploration.tsx`, `src/app/sandbox/kitchen-sink/cards/page.tsx`, `src/components/ui/grid-card.tsx`

---

## Addendum — Route lab content width + sandbox public chrome + `SandboxToolsShell`

### Context

- User **`LOG:`** after unifying route-lab screen bodies (**`RouteLabScreenBody`** / **`contentPreset`**), aligning route-lab with other sandbox tools, removing the **Tools** block from the route-lab sidebar, then moving sandbox to shared public top/footer with a flush-left sidebar and wider dev content column.

### Work Done

- **`RouteLabScreenBody`** + **`contentPreset`** on **`RouteLabPage`** (`auth` | `form` | `full`); scenario pages refactored (no duplicate headings; **`AuthContentContainer`** replaced by **`auth`** preset on public screens).
- **Route lab layout** briefly matched kitchen-sink **`PublicPageWrapper`**; superseded by sandbox-wide shell.
- **`SandboxRouteLabSidebar`:** removed **Tools** section (**`SANDBOX_PORTAL_LINKS`**); header uses **Typography** like other labs; **`w-64`** aside.
- **`PublicPageWrapper`:** **`contentAs?: "main" | "div"`**; vertical padding **`py-12`** only for **`main`** content slot.
- **`src/app/sandbox/layout.tsx`:** **`DevSandboxGate`** → **`PublicPageWrapper`** **`contentAs="div"`** (public bar + footer for all **`/sandbox/*`**).
- **`SandboxToolsShell`:** full-width **`flex`** row, **`main`** with wide inner **`max-w-[min(100%,92rem)]`**; used by kitchen-sink, interaction-lab, route-lab layouts (removed per-tool **`PublicShellContainer`** so sidebar is flush left).
- **`/sandbox` portal:** **`PublicShellContainer`** + **`py-8 md:py-12`** for spacing under **`py-0`** sandbox content slot.

### Results

- Portal and tools share marketing chrome; dev tools get a wider canvas; route-lab navigation focuses on route-lab screens only.

### Next

- [ ] Browser smoke **`/sandbox`**, **`/sandbox/route-lab/*`**, **`/sandbox/kitchen-sink`** (layout + padding).
- [ ] Optional: align **`.comms`** sandbox path examples if any still show pre-unification layout notes.

### Links

- `src/components/dev/route-lab-screen-body.tsx`, `src/components/dev/RouteLabPage.tsx`, `src/components/dev/SandboxRouteLabSidebar.tsx`, `src/components/dev/sandbox-tools-shell.tsx`
- `src/components/auth/layout.tsx` (`PublicPageWrapper`, `contentAs`)
- `src/app/sandbox/layout.tsx`, `src/app/sandbox/page.tsx`, `src/app/sandbox/kitchen-sink/layout.tsx`, `src/app/sandbox/interaction-lab/layout.tsx`, `src/app/sandbox/route-lab/layout.tsx`

---

## Addendum — Feedback cards: reusable UI + skills

### Context

- Comms spec [`src/app/sandbox/kitchen-sink/cards/.comms/Feedback Cards Expansion.md`](src/app/sandbox/kitchen-sink/cards/.comms/Feedback%20Cards%20Expansion.md) for persistent semantic cards on **`/sandbox/kitchen-sink/cards`**; user asked to extract variants into **`src/components/ui`** and update skills.

### Work Done

- **`src/components/ui/feedback-card.tsx`:** **`FeedbackCard`** ( **`visualVariant`** `soft` | `tinted` | `strong` ), **`FeedbackCardSoft`**, **`FeedbackCardTinted`**, **`FeedbackCardStrong`**; props for **`kind`**, copy, CTAs, optional metadata/dismiss/actions; semantic styling aligned with existing **`Card`** tokens.
- **`src/app/sandbox/kitchen-sink/cards/feedback-cards-section.tsx`:** kitchen sink section only; maps demo data onto the three wrappers.
- **`src/app/sandbox/kitchen-sink/cards/page.tsx`:** imports **`FeedbackCardsSection`** between Complex Composition and **`GridCardExplorationSection`**.
- **`.skills`:** **`component-Usage-Patterns.md`** (examples + responsibility list), **`layout-and-Spacing-System.md`** (Card Container bullet), **`feedback-and-Notifications.md`** (§4.4 persistent feedback cards + kitchen sink reference), **`kitchen-Sink-Maintenance.md`** (cards line mentions feedback + GridCard).

### Results

- Approved pattern for in-page persistent feedback lives in one module; agents can discover it via skills and the kitchen sink.

### Next

- [ ] Optional: browser smoke **`/sandbox/kitchen-sink/cards`** (feedback + GridCard sections).
- [ ] Use **`FeedbackCard*`** on feature pages when product needs persistent semantic messaging (not toasts).

### Links

- `src/components/ui/feedback-card.tsx`, `src/app/sandbox/kitchen-sink/cards/feedback-cards-section.tsx`, `src/app/sandbox/kitchen-sink/cards/page.tsx`
- `.skills/component-Usage-Patterns.md`, `.skills/layout-and-Spacing-System.md`, `.skills/feedback-and-Notifications.md`, `.skills/kitchen-Sink-Maintenance.md`

---

## Addendum — Scoped dashboard: temp org aggregate JSON dump

### Context

- Remove demo filler from **`/o/[accountId]/dashboard`**; surface **`GET /api/account/organisation/[accountId]`** payload for development so the team can map fields across the app before building real UI.

### Work Done

- Removed **`SectionCards`**, **`ChartAreaInteractive`**, **`DataTable`**, **`SessionApiCallout`**, and **`data.json`** from [`src/app/(members)/o/[accountId]/dashboard/page.tsx`](<src/app/(members)/o/[accountId]/dashboard/page.tsx>).
- Added isolated client module [`src/app/(members)/o/[accountId]/dashboard/temp-data-drilling/temp-org-data-dump.tsx`](<src/app/(members)/o/[accountId]/dashboard/temp-data-drilling/temp-org-data-dump.tsx>): **`useAccountOrganisation(accountId)`**, branches for loading / error + retry / gateway redirect edge case, success **`JSON.stringify`** in a scrollable **`<pre>`**; only **`page.tsx`** imports this folder (no barrel).
- Async server page passes **`accountId`** from **`await params`**; short title copy marks the view as temporary.

### Results

- Dashboard shows live organisation aggregate JSON when access boundary succeeds; React Query cache is shared with **`OrgAccessBoundary`** (no extra fetch in the normal success path).

### Next

- [ ] Replace or remove **`temp-data-drilling`** when real dashboard UI ships; slice **`OrganisationAccountDetailsData`** into typed consumers as screens need fields.

### Links

- `src/app/(members)/o/[accountId]/dashboard/page.tsx`, `src/app/(members)/o/[accountId]/dashboard/temp-data-drilling/temp-org-data-dump.tsx`
- `src/lib/api/hooks/account/useAccountOrganisation.ts`, `src/components/auth/org-access-boundary.tsx`
