# Episode — 2026-04-06

## Context

- User asked to implement the Phase 0 handoff plan (CMS contract only; no new HTTP handlers) and then triggered **`LOG:`** to record outcomes.

## Work Done

- Documented Phase 0 scope in `src/lib/api/routes/route-definitions.ts`: dedicated `/api/accounts/...` routes remain **`planned`** until matching backend phases ship.
- Added **`appRoutes.accounts`** (settings, branding, organisation, scheduler, render token, renders, render detail, analytics overview) using base path `/api/accounts` and descriptions for path suffixes built in services.
- Added `src/lib/api/account-scoped-http-semantics.ts` (`accountScopedHttpSemantics`) aligned with Phase 0 error table for future hooks/UI.
- Added `.comms/data-fetching/account-admin-api-contract.md` so handoff links resolve; includes §2–§7, §9–§15, route map, and explicit HTML anchors matching existing handoff fragment links.
- **`npm run lint`** passed after changes.

## Decisions

- See `.memory/decisions.md` — **2026-04-06 — Account-scoped CMS routes: planned registry + contract doc**.

## Results

- Phase 0 is encoded in-repo without flipping any account-scoped route to **`ready`** or adding new TanStack hooks until handlers exist per phase handoffs.
- `src/app/api/admin/fetch-health/route.ts` will list the new planned routes as **skipped** in diagnostics (expected).

## Next

- [ ] When a phase backend is live, set the matching `appRoutes.accounts.*` entry to **`ready`**, implement `src/lib/api/services/account.api.ts` + query keys + hooks per `.skills/api-data-layer-patterns.md`.
- [ ] Use `accountScopedHttpSemantics` when mapping account-scoped API errors to UI.

## Links

- `.comms/data-fetching/handoff/handoff-phase-00-contract.md`
- `.comms/data-fetching/account-admin-api-contract.md`

---

## Follow-up — `select-organisation-sim` lint

### Context

- ESLint reported unused import `AccountMePayload` in `src/lib/dev/select-organisation-sim.ts`.

### Work Done

- Removed `AccountMePayload` from the type import; file only uses `AccountMeResponse` and `AccountSummary`.

### Results

- `no-unused-vars` / `@typescript-eslint/no-unused-vars` resolved for that file.

### Next

- [ ] (unchanged from main episode) CMS phases → flip routes, services, hooks.

---

## Follow-up — Phase 8: `GET /accounts/:accountId/renders/:renderId`

### Context

- Implement the Phase 8 handoff (render detail for job detail view): BFF, types, `accountApi`, TanStack hook, registry **`ready`**, per plan.

### Work Done

- Added types: `AccountRenderDetailDownload`, `AccountRenderDetail`, `AccountRenderDetailData`, `AccountRenderDetailResponse` in `src/types/api/account.ts`.
- Added `isValidRenderIdSegment` in `src/lib/config/account-routes.ts`; BFF `src/app/api/accounts/[accountId]/renders/[renderId]/route.ts` proxies to Strapi with cookie JWT.
- Extended `accountApi` with `getAccountRenderDetail`; `queryKeys.account.renderDetail`; hook `useAccountRenderDetail` with gateway redirect pattern (`selectOrgReasonFromApiStatus`).
- Set `appRoutes.accounts.renderDetail` to **`ready`** in `route-definitions.ts`.
- **`npm run typecheck`** and ESLint on touched files passed.

### Results

- Members-area data layer can fetch single-render detail without the legacy hub; production job-detail UI route not wired yet (optional follow-up).

### Next

- [ ] Smoke BFF `/api/accounts/{accountId}/renders/{renderId}` against live CMS with `getAccountRenderDetail` permission enabled.
- [ ] Add `/o/[accountId]/...` render job detail page when UX is ready, consuming `useAccountRenderDetail`.

### Links

- `.comms/data-fetching/handoff/handoff-phase-08-accounts-render-detail.md`
- `src/lib/api/hooks/account/useAccountRenderDetail.ts`

---

## Follow-up — Phase 7: `GET /accounts/:accountId/renders` (list)

### Context

- Implement the Phase 7 handoff (paginated, filterable render history): registry **`ready`**, BFF, types, `accountApi`, TanStack hook, per plan.

### Work Done

- Set `appRoutes.accounts.renders` to **`ready`** in `src/lib/api/routes/route-definitions.ts`.
- Added BFF `src/app/api/accounts/[accountId]/renders/route.ts` (forwards query string to Strapi; cookie JWT).
- Added types `AccountRenderListRow`, `AccountRendersListResponse`, `AccountRendersListParams`, etc. in `src/types/api/account.ts`.
- Extended `accountApi` with `getAccountRenders`; `queryKeys.account.renders(accountId, params)` in `src/lib/api/query/query-keys.ts`.
- Added `useAccountRenders` (`src/lib/api/hooks/account/useAccountRenders.ts`) using `selectOrgReasonFromApiStatusExcludingBadRequest` (403/404 gateway only; 400 stays a query error).
- Added `selectOrgReasonFromApiStatusExcludingBadRequest` in `src/lib/config/gateway-reasons.ts` and a unit test in `gateway-reasons.test.ts`.
- **`npm run typecheck`** and **`npm test`** passed.

### Results

- Members-area data layer can load paginated render lists without the legacy hub for that table; dedicated renders UI page not required for this slice (optional later).

### Next

- [ ] Smoke BFF `GET /api/accounts/{accountId}/renders` against live CMS with **`getAccountRenders`** enabled for Authenticated.
- [ ] When building render history UI, use **`useAccountRenders`** only (do not rely on hub `*_count` fields on this endpoint).

### Links

- `.comms/data-fetching/handoff/handoff-phase-07-renders-list.md`
- `src/lib/api/hooks/account/useAccountRenders.ts`

---

## Follow-up — Phase 6: `GET /accounts/:accountId/render-token`

### Context

- Implement Phase 6 handoff (explicit render-token fetch; keep `render_token` off bootstrap `/account/me`): registry **`ready`**, BFF, types, `accountApi`, TanStack hook, per plan.

### Work Done

- Set `appRoutes.accounts.renderToken` to **`ready`** in `src/lib/api/routes/route-definitions.ts`.
- Added BFF `src/app/api/accounts/[accountId]/render-token/route.ts` (cookie JWT → Strapi; `cache: "no-store"`).
- Added types `AccountRenderTokenDocument`, `AccountRenderTokenData`, `AccountRenderTokenResponse` in `src/types/api/account.ts` (JSDoc: secret `token`).
- Extended `accountApi` with `getAccountRenderToken`; `queryKeys.account.renderToken`; hook `useAccountRenderToken` with gateway redirect pattern and `staleTime: 0`.
- No members screen wired yet (plan: optional until product needs the credential).

### Results

- Members-area data layer can fetch sanitized render-token without the legacy hub aggregate; no UI consumer added.

### Next

- [ ] Smoke BFF `GET /api/accounts/{accountId}/render-token` against live CMS with **`getAccountRenderToken`** enabled for Authenticated.
- [ ] When a screen needs the credential, use **`useAccountRenderToken`** (do not add `render_token` to `/account/me`).

### Links

- `.comms/data-fetching/handoff/handoff-phase-06-accounts-render-token.md`
- `src/lib/api/hooks/account/useAccountRenderToken.ts`

---

## Follow-up — Phase 5: `GET /accounts/:accountId/scheduler`

### Context

- Implement Phase 5 handoff (read-only scheduler document, `Queued` / `isRendering`, `days_of_the_week`; not for renders or `isUpdating`): registry **`ready`**, BFF, types, `accountApi`, TanStack hook, per plan.

### Work Done

- Set `appRoutes.accounts.scheduler` to **`ready`** in `src/lib/api/routes/route-definitions.ts`.
- Added BFF `src/app/api/accounts/[accountId]/scheduler/route.ts` (cookie JWT → Strapi; `cache: "no-store"`).
- Added types `AccountSchedulerDayOfWeek`, `AccountSchedulerDocument`, `AccountSchedulerData`, `AccountSchedulerResponse` in `src/types/api/account.ts`.
- Extended `accountApi` with `getAccountScheduler`; `queryKeys.account.scheduler`; hook `useAccountScheduler` with gateway redirect pattern (`isAccountSchedulerGatewayRedirect`).
- No members screen wired yet (plan: optional until scheduler UX ships).

### Results

- Members-area data layer can load scheduler config and flags without the legacy hub for that slice; render history remains Phase 7 / hub; `isUpdating` stays on settings.

### Next

- [ ] Smoke BFF `GET /api/accounts/{accountId}/scheduler` against live CMS with **`getAccountScheduler`** enabled for Authenticated.
- [ ] When building scheduler UI, use **`useAccountScheduler`** + **`useAccountSettings`** for `isUpdating` (not this endpoint).

### Links

- `.comms/data-fetching/handoff/handoff-phase-05-accounts-scheduler.md`
- `src/lib/api/hooks/account/useAccountScheduler.ts`

---

## Follow-up — Phase 4: `GET /accounts/:accountId/organisation`

### Context

- Implement Phase 4 handoff (read-only club/association summary; canonical scoped org context; legacy hub unchanged for heavy aggregate).

### Work Done

- Set `appRoutes.accounts.organisation` to **`ready`** in `src/lib/api/routes/route-definitions.ts`.
- Added types `AccountOrganisationContextData` / `AccountOrganisationContextResponse` and optional `PlayHQID` on `AccountOrganisationDetails` in `src/types/api/account.ts`.
- Added BFF `src/app/api/accounts/[accountId]/organisation/route.ts` (cookie JWT → Strapi).
- Extended `accountApi` with `getAccountOrganisationContext`; `queryKeys.account.organisationContext` (separate from legacy hub key `account.organisation`).
- Added `useAccountOrganisationContext` with gateway redirect guard (`isAccountOrganisationContextGatewayRedirect`).
- **`OrgAccessBoundary`** and scoped **`AppSidebar`** switched from hub **`useAccountOrganisation`** to Phase 4 hook; sidebar member name from bootstrap **`useAccountMe`** / account row.
- Left **`temp-org-data-dump`** on legacy **`useAccountOrganisation`** (hub aggregate).
- **`npm run typecheck`** and **`npm test`** passed.

### Results

- Scoped routes validate access and sidebar org display via the dedicated slice without loading the full hub; hub remains available for drill-down and future domains until later phases replace them.

### Next

- [ ] Smoke BFF `GET /api/accounts/{accountId}/organisation` against live CMS with **`getAccountOrganisation`** enabled for Authenticated (Strapi users-permissions).

### Links

- `.comms/data-fetching/handoff/handoff-phase-04-accounts-organisation.md`
- `src/lib/api/hooks/account/useAccountOrganisationContext.ts`

---

## Follow-up — Phase 3: `GET /accounts/:accountId/branding`

### Context

- Implement Phase 3 handoff (read-only template, theme, `template_option` for branding/preview; `hasCustomTemplate` remains on settings): registry **`ready`**, BFF, types, `accountApi`, TanStack hook, optional template-builder UI, per plan.

### Work Done

- Set `appRoutes.accounts.branding` to **`ready`** in `src/lib/api/routes/route-definitions.ts`.
- Added BFF `src/app/api/accounts/[accountId]/branding/route.ts` (cookie JWT → Strapi; `cache: "no-store"`).
- Added types `BrandingMedia`, `AccountBrandingTemplate`, `AccountBrandingTheme`, `AccountBrandingTemplateOption`, `AccountBrandingData`, `AccountBrandingResponse` in `src/types/api/account.ts`.
- Extended `accountApi` with `getAccountBranding`; `queryKeys.account.branding`; hook `useAccountBranding` with gateway redirect pattern (`isAccountBrandingGatewayRedirect`) and `staleTime: 0`.
- Wired **`TemplateBuilderContent`** on `/o/[accountId]/template-builder` (read-only summary; mirrors settings gateway UX).

### Results

- Members-area data layer can load branding/preview payloads without using the legacy hub for template/theme/option; settings remain the source for `hasCustomTemplate`.

### Next

- [ ] Smoke BFF `GET /api/accounts/{accountId}/branding` against live CMS with **`getAccountBranding`** enabled for Authenticated (Strapi users-permissions).

### Links

- `.comms/data-fetching/handoff/handoff-phase-03-accounts-branding.md`
- `src/lib/api/hooks/account/useAccountBranding.ts`
- `src/app/(members)/o/[accountId]/template-builder/template-builder-content.tsx`

---

## Follow-up — Phase 2: `GET /accounts/:accountId/settings`

### Context

- User asked to implement the Phase 2 handoff plan (canonical read-only account settings; BFF → service → TanStack hook; optional settings page demo) and then triggered **`LOG:`** to record outcomes.

### Work Done

- Added BFF `src/app/api/accounts/[accountId]/settings/route.ts` (cookie JWT → Strapi `GET /api/accounts/:accountId/settings`; `cache: "no-store"`).
- Set `appRoutes.accounts.settings` to **`ready`** in `src/lib/api/routes/route-definitions.ts`.
- Added `AccountSettingsData` / `AccountSettingsResponse` in `src/types/api/account.ts`.
- Extended `accountApi` with `getAccountSettings`; `queryKeys.account.settings(accountId)`; hook `useAccountSettings` with gateway redirect pattern (`isAccountSettingsGatewayRedirect`).
- Wired `src/app/(members)/o/[accountId]/settings/page.tsx` and `account-settings-content.tsx` (read-only sample fields).
- **`src/lib/account/account-me-rows.ts`:** synthetic picker row from legacy `accountId` only when `accountId > 0` so dev `orgSim` “none” (`accountId: 0`, empty `accounts`) yields zero rows; **`npm test`** green.

### Results

- Members-area data layer can load account settings flags without the hub for that slice; shell bootstrap remains **`GET /api/account/me`**. Visiting scoped settings runs hub (boundary) plus settings fetch until a future perf change.

### Next

- [ ] Smoke BFF `GET /api/accounts/{accountId}/settings` against live CMS with **`getAccountSettings`** enabled for Authenticated (Strapi users-permissions).

### Links

- `.comms/data-fetching/handoff/handoff-phase-02-accounts-settings.md`
- `src/lib/api/hooks/account/useAccountSettings.ts`
- `src/app/(members)/o/[accountId]/settings/account-settings-content.tsx`

---

## Follow-up — Phase 1: `GET /account/me` bootstrap alignment

### Context

- Implement the Phase 1 handoff plan: align the members-area app with the documented lightweight bootstrap contract (no top-level `contentHub` / `extended` on `/me`); shell uses `accounts[]` + `accountOrganisationDetails`; optional 404 UX left for later.

### Work Done

- Updated `appRoutes.account.me` description in `src/lib/api/routes/route-definitions.ts`.
- `AccountMePayload` in `src/types/api/account.ts`: optional `contentHub` / deprecated `extended`; JSDoc for Phase 1 bootstrap.
- `src/lib/account/account-me-rows.ts`: `activeAccountSummaryFromMePayload`; legacy fallback rows only attach `contentHub` when present.
- `src/components/app-sidebar.tsx`: nav user chip from bootstrap row + `organisationDetailsFromAccountRow` (not top-level `contentHub`).
- `accountApi.getAccountMe()` / `useAccountMe(options?)` — removed `depth=extended`; BFF `src/app/api/account/me/route.ts` proxies plain `/api/account/me`.
- `select-organisation-content.tsx` and `src/lib/dev/select-organisation-sim.ts` updated; extended `account-me-rows.test.ts`.
- Verified: `vitest` on `account-me-rows.test.ts`, `tsc --noEmit`.

### Results

- Sidebar and types match Phase 1 bootstrap; legacy hub remains for heavy aggregate via existing organisation routes/hooks.

### Next

- [ ] Optional: map **404** from `GET /account/me` to clearer “no organisation linked” copy in `AccountLoadErrorFeedback` if fetch client exposes status.

### Links

- `.comms/data-fetching/handoff/handoff-phase-01-account-me-bootstrap.md`
- `src/lib/account/account-me-rows.ts`
- `src/components/app-sidebar.tsx`

---

## Follow-up — Phase 9: `GET /accounts/:accountId/analytics/overview`

### Context

- Implement Phase 9 handoff (range-scoped analytics KPIs, `metricsOverTime` per-day arrays, `series`; `{ data, meta }` envelope): registry **`ready`**, types, `accountApi`, query keys, TanStack hook, dev dashboard dual JSON panels; per plan.

### Work Done

- Added Phase 9 types in `src/types/api/account.ts` (`AccountAnalyticsOverviewParams`, `AccountAnalyticsOverviewResponse`, rollup/metrics/series shapes).
- Set `appRoutes.accounts.analyticsOverview` to **`ready`** in `src/lib/api/routes/route-definitions.ts`.
- Extended `accountApi` with `getAccountAnalyticsOverview`; `queryKeys.account.analyticsOverview(accountId, params)` in `src/lib/api/query/query-keys.ts`.
- Added `useAccountAnalyticsOverview` (`src/lib/api/hooks/account/useAccountAnalyticsOverview.ts`) with `selectOrgReasonFromApiStatusExcludingBadRequest` (400 = query error; 403/404 = gateway marker); `isAccountAnalyticsOverviewGatewayRedirect`.
- Updated `temp-org-data-dump.tsx` to show Phase 9 analytics JSON and legacy hub JSON side by side; `dashboard/page.tsx` subtitle adjusted.
- **`npm run typecheck`** and ESLint on touched files passed.

### Results

- Members-area data layer can load bounded-window analytics without deriving rollups from the paginated renders list; bootstrap `/account/me` unchanged.

### Next

- [ ] Smoke `GET /api/accounts/{accountId}/analytics/overview` (BFF or Strapi) with **`getAccountAnalyticsOverview`** enabled for Authenticated; confirm `meta` and empty-scheduler zeros.

### Links

- `.comms/data-fetching/handoff/handoff-phase-09-analytics-overview.md`
- `src/lib/api/hooks/account/useAccountAnalyticsOverview.ts`

---

## Follow-up — Dev API dumps, Bundles nav, Settings `/me`, Branding route

### Context

- User wanted dashboard visibility for CMS handoff endpoints; then moved Phases 5–8 to **Bundles**, duplicated **Phase 1** `GET /api/account/me` on **Settings** (keeping dashboard), and added a **Branding** nav item with a dedicated route showing Phase 3 branding JSON.

### Work Done

- **`dashboard/temp-data-drilling/dump-block.tsx`:** shared `DumpBlock` + `jsonStringifyRedactingToken` for dumps.
- **`temp-org-data-dump.tsx`:** Phases **1–4**, **9**, and **legacy hub** only (removed 5–8 after move).
- **`bundles/bundles-api-dump.tsx` + `bundles/page.tsx`:** Phases **5–8** (Phase 8 sample render id **`8595`**); token field redacted in Phase 6 JSON.
- **`settings/settings-account-me-dump.tsx` + `settings/page.tsx`:** **Phase 1** `/api/account/me` JSON above account settings content (dashboard Phase 1 block retained).
- **`branding/branding-api-dump.tsx` + `branding/page.tsx`:** **Phase 3** `GET /api/accounts/:id/branding` dump.
- **`account-routes.ts`:** `branding` segment → `accountScopedRoutes.branding`.
- **`app-sidebar.tsx`:** "Content Bundles" → **"Bundles"**; **Branding** (`IconPalette`) between Bundles and Templates.
- **`site-header.tsx`:** titles for Bundles / Branding.
- **`middleware.ts`**, **`safe-return-path.ts`** (+ test): **`branding`** segment allowlist and legacy `/branding` handling.

### Results

- Scoped members can inspect bootstrap + settings/org/branding/analytics + hub on **Dashboard**; scheduler through render-detail samples on **Bundles**; bootstrap JSON also on **Settings**; branding-only JSON on **`/o/[accountId]/branding`**.

### Next

- [ ] Replace temp JSON panels with real dashboard, bundles, and branding UIs when designs are ready.
- [ ] Optional: remove duplicate Phase 3 block from dashboard if Branding page is the single source of truth for that dump.

### Links

- `src/app/(members)/o/[accountId]/dashboard/temp-data-drilling/temp-org-data-dump.tsx`
- `src/app/(members)/o/[accountId]/bundles/bundles-api-dump.tsx`
- `src/app/(members)/o/[accountId]/settings/settings-account-me-dump.tsx`
- `src/app/(members)/o/[accountId]/branding/branding-api-dump.tsx`
- `src/components/app-sidebar.tsx`

---

## Follow-up — Bundles API dump: Phase 8 `renderId` from renders list

### Context

- `GET /api/accounts/443/renders/8595` returned **404** while list and scheduler succeeded; investigation showed **`443`** comes from the members route **`/o/[accountId]/bundles`**, and **`8595`** was a **hardcoded** Phase 8 sample id in **`bundles-api-dump.tsx`**, so detail could target a render not owned by the scoped account.

### Work Done

- Removed **`PHASE_8_SAMPLE_RENDER_ID`**; after a successful Phase 7 response, read **`data.renders[0]?.id`**, stringify as **`phase8RenderId`**, and call **`useAccountRenderDetail`** only when **`accountId`** and **`phase8RenderId`** are set.
- Empty page-1 list: show _“No renders on page 1 — Phase 8 detail request skipped.”_; Phase 8 **`isPending`** includes **`renders.isPending`**; **Retry** refetches list and detail.

### Results

- Dev dump Phase 8 matches the first render row for the **same** account as the list call, avoiding account/render id mismatches during manual testing.

### Next

- [ ] (unchanged) Smoke Phase 8 BFF against live CMS; optional job-detail UI with **`useAccountRenderDetail`**.

### Links

- `src/app/(members)/o/[accountId]/bundles/bundles-api-dump.tsx`

---

## Follow-up — All template options (`GET …/all-template-options`)

### Context

- Implement CMS handoff [`.comms/data-fetching/handoff/handoff-template-all-template-options.md`](.comms/data-fetching/handoff/handoff-template-all-template-options.md): full published template catalog in one call, optional `templateOptionId` for `currentSelection`; GET-only (no PUT). Confirmation UI on `/o/[accountId]/template-builder`.

### Work Done

- BFF [`src/app/api/accounts/[accountId]/all-template-options/route.ts`](src/app/api/accounts/[accountId]/all-template-options/route.ts): proxies to Strapi `GET /api/template-categories/all-template-options` with `accountId` + optional validated `templateOptionId` (invalid → 400).
- [`route-definitions.ts`](src/lib/api/routes/route-definitions.ts): `accounts.allTemplateOptions` (**ready**).
- [`account.api.ts`](src/lib/api/services/account.api.ts): `getAllTemplateOptions`; [`query-keys.ts`](src/lib/api/query/query-keys.ts): `account.allTemplateOptions`.
- [`account.ts`](src/types/api/account.ts): `AllTemplateOptionsData` / `AllTemplateOptionsResponse` / `AllTemplateOptionsParams`; `templateOptionId` on `AccountSummary` and `AccountBrandingData`.
- [`useAllTemplateOptions.ts`](src/lib/api/hooks/account/useAllTemplateOptions.ts): gateway redirect pattern for 400/403/404 (`exactOptionalPropertyTypes`-safe keys).
- [`template-builder-content.tsx`](<src/app/(members)/o/[accountId]/template-builder/template-builder-content.tsx>): resolves `templateOptionId` from `useAccountMe` (`accounts[]` row) then branding; dual redirect cleanup; branding card shows CMS `templateOptionId`.
- [`all-template-options-dump.tsx`](<src/app/(members)/o/[accountId]/template-builder/all-template-options-dump.tsx>): `DumpBlock` + counts summary + JSON (`jsonStringifyRedactingToken`).
- [`page.tsx`](<src/app/(members)/o/[accountId]/template-builder/page.tsx>): subtitle updated.
- **`npm run typecheck`** and **`npm test`** passed.

### Results

- Members-area can load the full template catalog and optional saved selection without guessing ids when CMS exposes `templateOptionId` on `/account/me` and branding. PUT/save deferred to a later phase.

### Next

- [ ] After CMS deploy: enable **`template-category` → `getAllTemplateOptions`** for **Authenticated** in Strapi (403 otherwise).
- [ ] Smoke BFF and `/o/{accountId}/template-builder` against live CMS.

### Links

- `.comms/data-fetching/handoff/handoff-template-all-template-options.md`
- `src/lib/api/hooks/account/useAllTemplateOptions.ts`
- `src/app/api/accounts/[accountId]/all-template-options/route.ts`

---

## LOG — `state.md` drift fix (Bundles Phase 8)

### Context

- **`LOG:`** re-triggered; **`state.md`** still described Bundles Phase 8 with a **hardcoded** sample render id (`8595`) after **`bundles-api-dump`** was updated to derive **`renderId`** from the Phase 7 list.

### Work Done

- **`state.md`:** Bundles bullet now states Phase 8 uses **`data.renders[0].id`** from the list response (matches follow-up _“Bundles API dump: Phase 8 `renderId` from renders list”_ in this episode).

### Results

- Current-focus snapshot matches implemented dev dump behaviour; no code change in this LOG step.
