# Episode — 2026-04-28

## Context

- Interaction lab calendar display (`/sandbox/interaction-lab/calendar/display`) scheduler UX and sandbox shell scrolling.

## Work Done

- Calendar display lab: removed Week view and `@fullcalendar/timegrid` usage from `FixtureCalendar`; emerald/green bundle badges (dense + list); replaced “Calendar Event Sandbox” `Card` with plain `div` + toggles; updated page metadata, selected-date copy, Test Areas badges; fixed `calendar-display-lab.test.tsx` to assert `radio` “Show all events” (vitest passes).
- Explained internal scrollbar: `SandboxToolsShell` `<main>` had `overflow-y-auto` beside `h-screen` sidebar, constraining main to viewport height.
- Removed `overflow-y-auto` from `SandboxToolsShell` main so content uses document scroll (interaction-lab, route-lab, kitchen-sink, data-lab).

## Decisions

- (See `/.memory/decisions.md` — sandbox shell scroll surface.)

## Results

- Single scroll surface for sandbox tool pages using `SandboxToolsShell`; sidebar nav still scrolls independently when the nav list exceeds the viewport.

## Next

- [ ] Optional: drop unused `@fullcalendar/timegrid` from `package.json` if no other imports remain.

## Links

- `src/app/sandbox/interaction-lab/calendar/display/_components/fixture-calendar.tsx`
- `src/app/sandbox/interaction-lab/calendar/display/_components/calendar-display-lab.tsx`
- `src/components/dev/sandbox-tools-shell.tsx`

---

## Session — Route-lab season overview cards & shared UI

### Context

- Polish sandbox competition summary cards on `/sandbox/route-lab/season/575/overview` and align with kitchen-sink card patterns; improve `MetricComparisonCard` and button variants.

### Work Done

- Added `MetricComparisonCard` (`surface` | `card` layouts) with banded header/body/footer and className slots; kitchen sink shows `card.metric.comparison` + `card.metric.comparison-card`; tests pass.
- Overview competition cards: `primary-950` header (white text), flush shell via `Card` `gap-0 p-0`, header/footer padding on sections; theme token `--color-primary-950` in `globals.css`.
- Stats use `card.metric.inline-info` surfaces; status `Badge` above grid, right-aligned; active → `bg-success-600`, not active → `bg-error-600`; theme tokens `--color-success-600`, `--color-error-600`.
- `CardContent` vertical padding `py-6` on overview summary cards.
- New `accentOutline` button variant; `SeasonRouteLabRowLink` “Open” uses `Button asChild` + inner `span` (valid inside wrapping `Link`).
- `SeasonRouteLabRowLink`: `Link` uses `w-full min-w-0`; footer layouts use `flex-col items-stretch` + footer `pb-6` with no `Card` root `pb-6` for full-bleed footer band.

### Decisions

- (None logged to `decisions.md`.)

### Results

- Overview route demonstrates full-bleed section bands, semantic status badge colors from brand scale, and accent outline CTA inside a single row link.

### Next

- [ ] Reuse `MetricComparisonCard` or the same `Card` flush pattern in product season UI when ready.

### Links

- `src/components/cards/MetricComparisonCard.tsx`
- `src/app/sandbox/route-lab/season/575/overview/page.tsx`
- `src/app/sandbox/route-lab/season/575/_components/season-route-lab-frame.tsx`
- `src/components/ui/button.tsx`
- `src/app/globals.css`

---

## Session — Kitchen Sink page headers boilerplate + variants

### Context

- Stand up a new `/sandbox/kitchen-sink/page-headers` route to catalog members-area title/header patterns, then start implementing variant previews and reference naming.

### Work Done

- Created `src/app/sandbox/kitchen-sink/page-headers/` with `page.tsx` and `_sections/` scaffolding for intro + variant sections.
- Wired the route into kitchen sink navigation and overview grid (`layout.tsx`, `kitchen-sink-overview-grid.tsx`).
- Seeded local docs and research for this feature folder:
  - `.docs/readMe.md`
  - `.docs/DevelopmentRoadMap.md`
  - `.docs/Tickets.md`
  - `.docs/Completed.md`
  - `.research/2026-04-28-page-header-variants.md`
- Implemented live preview sections for:
  - `page.header.basic`
  - `page.header.eyebrow`
  - `page.header.breadcrumbs`
  - `page.header.actions.trailing`
  - `page.header.meta.row`
  - `page.header.tabs.subnav`
  - `page.header.hero.stats`
  - `page.header.detail.back-link`
  - `page.header.search.inline`
- Added reusable copy-to-clipboard reference token component for page headers:
  - `page-header-reference-name.tsx`
  - Included in every section, matching card reference behavior.
- Added naming convention note to intro:
  - `page.header.<variant>[.<modifier>]`

### Decisions

- Introduced canonical page-header reference naming + copy token usage for kitchen sink variants (logged in `/.memory/decisions.md`).

### Results

- `/sandbox/kitchen-sink/page-headers` now serves as a practical implementation catalogue, not just TODO stubs.
- Header variants have consistent naming tokens that can be copied and reused in tickets, docs, and implementation discussions.

### Next

- [ ] Optionally add a single variant inventory block (all reference tokens listed together) at the top of the page.
- [ ] Start consuming one selected page-header variant in route-lab season UI (`TKT-2026-005`) when approved.

### Links

- `src/app/sandbox/kitchen-sink/page-headers/page.tsx`
- `src/app/sandbox/kitchen-sink/page-headers/page-header-reference-name.tsx`
- `src/app/sandbox/kitchen-sink/page-headers/_sections/basic.tsx`
- `src/app/sandbox/kitchen-sink/page-headers/_sections/eyebrow.tsx`
- `src/app/sandbox/kitchen-sink/page-headers/_sections/breadcrumbs.tsx`
- `src/app/sandbox/kitchen-sink/page-headers/_sections/actions.tsx`
- `src/app/sandbox/kitchen-sink/page-headers/_sections/meta.tsx`
- `src/app/sandbox/kitchen-sink/page-headers/_sections/tabs.tsx`
- `src/app/sandbox/kitchen-sink/page-headers/_sections/stats.tsx`
- `src/app/sandbox/kitchen-sink/page-headers/_sections/back.tsx`
- `src/app/sandbox/kitchen-sink/page-headers/_sections/search.tsx`

---

## Session — Route-lab season overview breadcrumb header + purpose copy

### Context

- Align `/sandbox/route-lab/season/575/overview` header to the `page.header.breadcrumbs` pattern and clarify page language around Fixtura tracking coverage and upcoming lookup-request CTAs.

### Work Done

- Added an optional `header` slot to `SeasonRouteLabFrame` so route pages can render a custom header stack while preserving existing default title/description behavior.
- Updated season overview to use breadcrumb-based header structure:
  - `Sandbox` -> `Route lab` -> `Season · 575 · Overview`
  - Title + supporting description beneath breadcrumbs.
- Refined overview copy to better match product purpose:
  - Header description now emphasizes tracked coverage visibility and readiness for fixture/competition/grade lookup requests.
  - Loading label updated to `Loading tracked season coverage...`.
  - Competition section title/help text reworded to coverage verification language.
  - Empty state and row-link CTA copy now describe tracking/coverage intent.
- Ran lints on touched route-lab files and resolved import-order warnings.

### Decisions

- (None logged to `decisions.md`.)

### Results

- The season overview route now uses the same breadcrumb-style page header pattern as the kitchen-sink reference and communicates a clearer user-facing purpose for tracking validation and upcoming request actions.

### Next

- [ ] Add explicit lookup-request CTA buttons/links (fixture, competition, grade) once product copy/flow is confirmed.
- [ ] Reuse the breadcrumb header slot pattern across other route-lab season pages for consistency.

### Links

- `src/app/sandbox/route-lab/season/575/_components/season-route-lab-frame.tsx`
- `src/app/sandbox/route-lab/season/575/overview/page.tsx`

---

## Session — Sections and dividers lab + route-lab season section composition

### Context

- Create a new kitchen-sink route for global section/divider patterns, then trial those patterns on `/sandbox/route-lab/season/575/overview`.

### Work Done

- Added shared UI primitives in `src/components/ui/section.tsx`:
  - `SectionBlock` (`plain` | `surface` | `inset`)
  - `SectionDivider` (`line` | `labeled` | `inset`)
  - `SectionLabel` (`eyebrow` | `kicker`)
- Added new route `src/app/sandbox/kitchen-sink/sections-and-dividers/page.tsx` with:
  - variant examples
  - reference-name copy controls
  - variant catalog for section, divider, and label handles.
- Wired discoverability updates:
  - Added `Sections & Dividers` to `src/app/sandbox/kitchen-sink/layout.tsx` nav links.
  - Added overview card entry in `src/app/sandbox/kitchen-sink/kitchen-sink-overview-grid.tsx`.
- Applied the new section system to route-lab season overview:
  - Wrapped season summary stats in `SectionBlock` with `variant="inset"`.
  - Wrapped tracked competitions block in the same inset wrapper style.
  - Positioned labeled `SectionDivider` between the two wrappers.
- Added alphabetical ordering for tracked competitions on `/sandbox/route-lab/season/575/overview` using case-insensitive name sort before rendering.
- Ran lint checks after each change set; no lint errors remained.

### Decisions

- (None logged to `decisions.md`.)

### Results

- Kitchen sink now has a dedicated sections/dividers reference route with reusable primitives available to product pages.
- Route-lab season overview now uses consistent section wrappers and divider rhythm, and competition cards display in predictable alphabetical order.

### Next

- [ ] Add lookup-request CTAs in tracked competitions/overview once product copy and actions are confirmed.
- [ ] Apply section wrapper/divider patterns across additional route-lab season pages where the same composition improves scannability.

### Links

- `src/components/ui/section.tsx`
- `src/app/sandbox/kitchen-sink/sections-and-dividers/page.tsx`
- `src/app/sandbox/kitchen-sink/layout.tsx`
- `src/app/sandbox/kitchen-sink/kitchen-sink-overview-grid.tsx`
- `src/app/sandbox/route-lab/season/575/overview/page.tsx`

---

## Session — Route-lab dynamic canonical fixture detail + payload normalization

### Context

- `/sandbox/route-lab/season/575/competitions/18031/grades/[gradeId]/fixtures/[fixtureId]` showed empty UI because fixture detail responses use varying envelopes (`json`, `data`, flattened DTO) and nested `teams` objects; align page with grade route-lab style and document in season route-lab review.

### Work Done

- Implemented full fixture detail lab page: custom `SeasonRouteLabFrame` header (six-step breadcrumb, Back/Sync, dual refetch for `useSeasonHubFixture` + `useSeasonHubGradeFixtures`), `grid gap-6` with labeled sections (match summary card, detail tiles, grade context, teams, outputs, context/meta), debug payload cards last; removed `console.info` for reversed fixtures.
- Extended `src/app/(members)/o/[accountId]/season/_components/_utils/season-fixture.ts`: `unwrapSeasonHubFixturePayload` (peel `json` / `data` / `attributes`, flattened fixture DTO detection, shallow value scan), stricter detail shape checks, `extractFixtureRecord` fallback for flat match records, `fixtureTeamSideLabel`, `resolveFixtureHeadline` prefers home vs away from nested team names.
- Updated `src/app/sandbox/route-lab/season/.docs/season-route-lab-full-review.md` (Document updates, Browser routes row, Data Layer fixture subsection, Dynamic Canonical Fixture page review, risks/reusable/verification).

### Decisions

- (Fixture payload normalization approach logged in `/.memory/decisions.md`.)

### Results

- Dynamic canonical fixture route renders real match data when the API returns wrapped or flattened shapes; shared helpers also benefit `SeasonFixtureView` via `extractFixtureRecord`.

### Next

- [ ] Bring fixed-ID and alias fixture lab pages to parity with the dynamic page when useful.
- [ ] Prefer a single canonical fixture-detail JSON shape from BFF/Strapi to simplify client normalization.

### Links

- `src/app/sandbox/route-lab/season/575/competitions/18031/grades/[gradeId]/fixtures/[fixtureId]/page.tsx`
- `src/app/(members)/o/[accountId]/season/_components/_utils/season-fixture.ts`
- `src/app/sandbox/route-lab/season/.docs/season-route-lab-full-review.md`
