# Episode — 2026-04-30

## Context

- Branding workspace: clear `@typescript-eslint/consistent-type-imports`, colocate `_consts` / `_types` / `_utils` / `_hooks`, trim default page header copy (no inline Template · Theme line).
- Responsive polish for `/o/[accountId]/branding`: clearer controls on narrow viewports, breakpoints (1024 / 1200), and simpler save confirmation.

## Work Done

- Replaced `import()` type annotations in `branding-workspace/_types/index.ts` with `import type`; workspace split from monolith into `index.tsx` + `_consts`, `_types`, `_utils`, `_hooks/use-branding-workspace.ts`; default `headerDescription` no longer appends template/theme line; removed `themeName` / `templateTitle` from view model.
- **Branding workspace layout:** Forms column first, asset preview second when stacked; two-column + sticky preview at `lg`+.
- **Brand `CardHeader`:** Full-width mode strip (no `1fr/auto` rail); **`min-[1200px]`** shows two CTAs, below that a **`Select`**; copy **“Create your brand colours”**.
- **Save branding:** For **`<=1024px`**, buttons below **`PageHeader`** and under preview (with success copy under preview); original bordered save row only **`>=1025px`**.
- **Preset theme grid:** **`grid-cols-2`** at smallest width; **`xl`/`2xl`** wider columns unchanged.
- **Contrast `ModeGridCards`:** **`grid-cols-2`** below **`min-[1200px]`**, four columns at **`1200px`+**.
- **Card footers:** **`lg:hidden`** line **“Preview the changes below”** after main footer copy on brand colours + contrast selector (ready state); contrast card footer wrapper uses **`gap-3`**.
- **Save dialog:** Removed slug and missing-slug lines; contrast block keeps mode label only.
- **`BrandColorField`:** Swatch + hex on one row at all sizes (`flex-row`, `min-w-0 flex-1` input).
- **`GridCard`:** Title rendered with **`span`** + merged classes (not **`TypographyCardTitle`**) so tile **`titleClassName`** sizes apply.
- **`PageHeader`:** `description` via conditional spread for **`exactOptionalPropertyTypes`**.
- **Members `BrandingScreen`:** Thin `branding-screen.tsx` + `useBrandingScreen`, `_types` / `_consts` / `_utils` / `_hooks` under `branding/_components/` (details in **Update** below).

## Decisions

- (none)

## Results

- Branding workspace types lint-clean; consumers keep the same import path; headers no longer show default Template · Theme fragment.
- Branding page improves on phones/tablets; save and colour-source breakpoints (1024 vs 1200) aligned with layout; confirmation dialog shorter.
- Production branding entry is screen-based; PATCH path and types are wired for palette/theme/template-mode persistence alongside the UX work above.

## Next

- [ ] Wire real branding save APIs in route-lab when graduating from fixture-backed lab stubs.

## Links

- `src/features/branding/components/branding-workspace/_types/index.ts`
- `src/features/branding/components/branding-workspace/index.tsx`
- `src/features/branding/components/branding-workspace/_hooks/use-branding-workspace.ts`
- `src/features/branding/components/branding-template-mode-cards-input/mode-grid-cards.tsx`
- `src/features/branding/components/branding-template-mode-cards-input/index.tsx`
- `src/features/branding/components/branding-template-mode-cards-input/contrast-selector-card.tsx`
- `src/components/brand-color/brand-color-field.tsx`
- `src/components/ui/grid-card.tsx`

---

## Update — Members `BrandingScreen` structure (same day)

### Context

- Align `BrandingScreen` for `/o/[accountId]/branding` with the same colocated `_consts` / `_types` / `_utils` / `_hooks` pattern as `branding-workspace`; keep redirect and query behaviour unchanged.

### Work Done

- Added `_types` (`BrandingScreenProps`, `BrandingScreenView`), `_consts` (copy + workspace defaults), `_utils` (`resolveBrandingScreenErrorDescription`, `brandingScreenViewFromQuery`), `_hooks/use-branding-screen.ts` under `src/app/(members)/o/[accountId]/branding/_components/`.
- Slim `branding-screen.tsx`: `useBrandingScreen` + `view.kind` rendering only; `page.tsx` import path unchanged.

### Decisions

- (none)

### Results

- Easier navigation and isolated branching for the members branding gate/loader/error/ready states; parity with pre-refactor behaviour.

### Next

- (same as episode **Next** above: route-lab branding save when APIs graduate.)

### Links

- `src/app/(members)/o/[accountId]/branding/_components/branding-screen.tsx`
- `src/app/(members)/o/[accountId]/branding/_components/_hooks/use-branding-screen.ts`
- `src/app/(members)/o/[accountId]/branding/_components/_utils/branding-screen-view-from-query.ts`

---

## Update — Members branding: single page header (same day)

### Context

- `/o/[accountId]/branding` showed two “Branding” blocks: one on the page and one inside the workspace `PageHeader`.

### Work Done

- `branding/page.tsx` now renders only `BrandingScreen` (no local `h1` / intro). Title and description are owned by the branding workspace.
- `BRANDING_PAGE_INTRO` and optional `includeBrandingPageIntro` live with the workspace (`_consts`, hook props) so route lab can pass `includeBrandingPageIntro={false}` and avoid repeating long copy beside `RouteLabPage`.

### Decisions

- (none)

### Results

- One header stack on members branding; no duplicate “Branding” + intro.

### Next

- [ ] (unchanged) Wire real branding save APIs in route-lab when graduating from fixture-backed lab stubs.

### Links

- `src/app/(members)/o/[accountId]/branding/page.tsx`
- `src/features/branding/components/branding-workspace/_consts/index.ts`
- `src/features/branding/components/branding-workspace/_hooks/use-branding-workspace.ts`
- `src/app/sandbox/route-lab/app/branding/page.tsx`

---

## Update — `BrandingTemplateModeCardsInput` colocated modules (same day)

### Context

- Shrink the feature component at `src/features/branding/components/branding-template-mode-cards-input` by mirroring the `template-mode` picker layout: `_types`, `_consts`, `_utils`, `_hooks`, plus thin presentational files; preserve the public import path via `index.tsx`.

### Work Done

- Replaced the monolithic `branding-template-mode-cards-input.tsx` with a folder: `_types` (props + `BrandingTemplateModeCardsInputState` union, card/grid props), `_consts` (`tile-layout.ts`, JSX footer copy in `contrast-selector-footer-copy.tsx`), `_utils` (`flatTileSelectedClass` / `flatTileSurfaceClass`), `_hooks/use-branding-template-mode-cards-input-state.ts` (always calls `useTemplateModePickerList`; returns `readonly` / `loading` / `error` / `empty` / `ready`).
- Extracted `ContrastSelectorCard` and `ModeGridCards` to sibling TSX modules; entry `index.tsx` switches on `state.phase`.
- Fixed `import/order` on `index.tsx` (type-import group + blank lines per ESLint config).
- Verified: ESLint on the folder is clean; full `tsc --noEmit` still fails on unrelated sandbox/other errors (none in this module).

### Decisions

- (none)

### Results

- Same behaviour and import paths for consumers (`branding-workspace`, sandbox re-export); contrast selector UI unchanged.

### Next

- [ ] Wire real branding save APIs in route-lab when graduating from fixture-backed lab stubs (unchanged).

### Links

- `src/features/branding/components/branding-template-mode-cards-input/index.tsx`
- `src/features/branding/components/branding-template-mode-cards-input/_hooks/use-branding-template-mode-cards-input-state.ts`
- `src/features/branding/components/branding-template-mode-cards-input/mode-grid-cards.tsx`
- `src/features/branding/components/branding-template-mode-cards-input/contrast-selector-card.tsx`

---

## Update — Members scoped sidebar: Brand Logo + grouped nav (same day)

### Context

- Extend `/o/[accountId]/` sidebar with a **Brand Logo** destination and clearer IA via labeled groups.

### Work Done

- Added `accountScopedRoutes.brandLogo` (`brand-logo` segment) and `src/app/(members)/o/[accountId]/brand-logo/page.tsx` rendering `BrandingScreen` (same UX as `/branding` until a logo-only screen exists).
- Refactored `NavMain` to accept **`sections`**: `{ label?: string; items[] }[]` with `SidebarGroupLabel` per labeled section; exported `NavMainItem`.
- **`AppSidebar` (scoped):** **Settings** (Settings item — moved from secondary), **Account** (Dashboard, Bundles, Billing, Season), **Assets** (Branding, Brand Logo, Templates, Media Gallery, Sponsors). **Get Help** only in `NavSecondary`.

### Decisions

- (none)

### Results

- Sidebar shows three category labels; Brand Logo has its own path and active state; Settings no longer duplicated in the footer secondary block.

### Next

- [ ] (unchanged) Wire real branding save APIs in route-lab when graduating from fixture-backed lab stubs.
- [ ] Optional: dedicated logo-only workspace on `/brand-logo` vs full `BrandingScreen`.

### Links

- `src/lib/config/account-routes.ts`
- `src/app/(members)/o/[accountId]/brand-logo/page.tsx`
- `src/components/nav-main.tsx`
- `src/components/app-sidebar.tsx`

---

## Update — Route lab logo uploader header (same day)

### Context

- `/sandbox/route-lab/app/logo-uploader`: the uploader block needed a clear title plus a primary-colour header band; the band should run to the card edges (no inset gutter).

### Work Done

- Added **Upload logo** title and moved the PNG/JPEG/WebP helper copy into a **`bg-primary-950`** / **`text-white`** strip with **`border-b border-white/15`**.
- Restructured the card: **`gap-0 overflow-hidden p-0`** on **`Card`**, **`p-0`** on **`CardContent`**, with **`px-6`** / **`py-6`** only on the recrop note, uploader body, non-interactive copy, and footer so the strip is full width; **`CardFooter`** gets explicit **`pb-6 pt-6`**.
- Dropped **`helperText`** from **`ImageUploaderCrop`** to avoid duplicate copy; set **`editableSourceUrl={editableLogoSourceUrl ?? null}`** for **`exactOptionalPropertyTypes`**.

### Decisions

- (none)

### Results

- Header presents as a full-width band inside the rounded card; **`overflow-hidden`** on the card clips corners cleanly.

### Next

- [ ] (unchanged) Wire real branding save APIs in route-lab when graduating from fixture-backed lab stubs.

### Links

- `src/app/sandbox/route-lab/app/logo-uploader/_components/logo-uploader-lab-workspace.tsx`

---

## Update — Production Brand Logo on `/o/[accountId]/brand-logo` (same day)

### Context

- Ship logo upload/crop/preview/save from route lab to production on `brand-logo`, using CMS M1 + W2 (see `.comms/responses/brand-logo-cms-fed-briefing.md`), not `PATCH /branding`.

### Work Done

- Added `src/features/branding/components/brand-logo-workspace/` (`index.tsx`, `logo-save-validation.ts`, `onboarding-step2-logo-error-message.ts`): `ImageUploaderCrop` with `editableSourceUrl` from `onboardingLogo.url`, min dimension hints (500×500 / 400×400), save via `useUpdateOnboardingStep2`, remove via `logoMediaId: null`, Strapi error code mapping for toasts.
- Added `brand-logo/_components/brand-logo-screen.tsx` + `_consts`; screen reuses `useBrandingScreen` (same gateway/loader/error as branding).
- `brand-logo/page.tsx` renders `BrandLogoScreen` (replaces duplicate `BrandingScreen`).
- Route lab `logo-uploader/page.tsx`: `productionRoute` uses `accountScopedRoutes.brandLogo("575")`.

### Decisions

- (logged in `.memory/decisions.md` — M1+W2 for production brand logo.)

### Results

- `/brand-logo` is logo-only; `/branding` remains full colours + template workspace.

### Next

- [ ] Manual QA in staging: save, recrop from saved URL, remove logo; watch CORS/canvas if media is cross-origin.
- [ ] Confirm with CMS post-onboarding M1/W2 policy (briefing §1.1) before relying on prod Strapi.

### Links

- `src/features/branding/components/brand-logo-workspace/index.tsx`
- `src/app/(members)/o/[accountId]/brand-logo/_components/brand-logo-screen.tsx`
- `src/app/(members)/o/[accountId]/brand-logo/page.tsx`
- `.comms/responses/brand-logo-cms-fed-briefing.md`
