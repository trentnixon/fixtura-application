# Episode — 2026-04-14

## Context

- Align the Remotion interaction lab route with sandbox typography and card patterns, and reserve a right column for upcoming filters.

## Work Done

- Refactored [`src/app/sandbox/interaction-lab/remotion/page.tsx`](src/app/sandbox/interaction-lab/remotion/page.tsx): metadata `Remotion preview — Interaction lab`; page header with eyebrow **Remotion**, `TypographyH1`, `TypographyInlineCode` for identifiers; **Complex Composition** `Card` (gradient strip, two-column layout) matching kitchen-sink [`cards/page.tsx`](src/app/sandbox/kitchen-sink/cards/page.tsx); left column for preview, right rail **Filters** placeholder.
- Updated [`src/components/remotion/remotion-preview.tsx`](src/components/remotion/remotion-preview.tsx): optional `className` merged with `cn()` onto the outer shell; default styling unchanged for other callers; lab passes overrides to avoid double border inside the card.

## Decisions

- None recorded (layout follows existing kitchen-sink **Complex Composition** pattern).

## Results

- `/sandbox/interaction-lab/remotion` matches other interaction-lab pages for type hierarchy and uses the shared Complex Composition card pattern with space for future filter UI.

## Next

- [ ] Replace the Filters placeholder in the Remotion lab right rail with real composition/dataset controls when ready.

## Links

- [`src/app/sandbox/interaction-lab/remotion/page.tsx`](src/app/sandbox/interaction-lab/remotion/page.tsx)
- [`src/components/remotion/remotion-preview.tsx`](src/components/remotion/remotion-preview.tsx)

---

## Session addendum — Remotion under Interaction lab (routing)

### Context

- Remotion preview was moved from a dedicated top-level sandbox route to the Interaction lab tree (`/sandbox/interaction-lab/remotion`).

### Work Done

- Set [`ROUTES.remotionPreview`](src/lib/config/routes.ts) to `"/sandbox/interaction-lab/remotion"` (single canonical URL).
- Registered **Remotion → Preview** in [`INTERACTION_LAB_NAV_SECTIONS`](src/lib/dev-sandbox-nav.ts) using `ROUTES.remotionPreview`.
- Confirmed [`SANDBOX_PORTAL_LINKS`](src/lib/dev-sandbox-nav.ts) / [`sandbox-portal-grid.tsx`](src/app/sandbox/sandbox-portal-grid.tsx) remain three tiles only (no separate Remotion portal entry).

### Decisions

- None (URL constant + sidebar entry only).

### Results

- Deep links and nav stay aligned; Remotion is discovered via Interaction lab sidebar, not the `/sandbox` portal grid.

### Next

- [ ] (unchanged) Filters placeholder on Remotion lab page — replace when composition/dataset controls exist.

### Links

- [`src/lib/config/routes.ts`](src/lib/config/routes.ts)
- [`src/lib/dev-sandbox-nav.ts`](src/lib/dev-sandbox-nav.ts)

---

## Session addendum — Remotion Player: handoff-aligned composition size

### Context

- Align `@remotion/player` + `FixturaTemplateScene` with the official Remotion package handoff (1080×1350) so the sandbox preview renders at the correct aspect ratio.

### Work Done

- [`src/components/remotion/remotion-preview.tsx`](src/components/remotion/remotion-preview.tsx): `COMPOSITION_HEIGHT` **1920 → 1350**; shell layout retuned (`max-w-3xl`, responsive `scale`, `Player` fills container).
- [`.comms/REMOTION_APP_INSTALL_GUIDE.md`](../../.comms/REMOTION_APP_INSTALL_GUIDE.md): canonical link to handoff; example updated (`FixturaTemplateScene`, `getProductionCompositionFromData`, `compositionHeight={1350}`, `inputProps={{ data }}`).
- Verification: `npx tsc --noEmit` passed; full `npm run build` optional follow-up.

### Decisions

- See [`.memory/decisions.md`](../decisions.md) — **2026-04-14 — Remotion in-app preview: composition matches `@fixtura/remotion-assets` handoff**.

### Results

- Preview canvas matches package-authored 4:5 frame; install guide no longer documents 1920 height for this flow.

### Next

- [ ] Run `npm run build` and smoke-test `/sandbox/interaction-lab/remotion` if not already done.

### Links

- [`.comms/Guide to Remotion Set up handoff.md`](../../.comms/Guide%20to%20Remotion%20Set%20up%20handoff.md)

---

## Session addendum — Remotion Player: responsive aspect box (card clipping fix)

### Context

- The sandbox `RemotionPreview` used a fixed **1080×1350px** DOM wrapper inside a **`max-h-[85vh]`** shell with **`overflow-hidden`**, so the player was vertically clipped (middle band visible) in the Interaction lab card.

### Work Done

- [`src/components/remotion/remotion-preview.tsx`](../../src/components/remotion/remotion-preview.tsx): replaced fixed pixel wrapper with **`aspect-[1080/1350]`** + **`max-w-[min(100%,calc(85vh*1080/1350))]`** + **`w-full`** so the composition scales to fit the viewport/card; shell no longer uses **`max-h-[85vh]`** / **`overflow-hidden`** for clipping; logical **`compositionWidth` / `compositionHeight`** remain **1080 / 1350**.
- Verification: `npm run typecheck`; `npx eslint src/components/remotion/remotion-preview.tsx`.

### Decisions

- None (layout/CSS-only; follows attached plan for responsive aspect sizing).

### Results

- Full player (preview + controls) should render scaled within the card without top/bottom cut-off from overflow clipping.

### Next

- [ ] Optional: run `npm run build` and smoke-test `/sandbox/interaction-lab/remotion` at narrow/wide viewports.

### Links

- [`src/components/remotion/remotion-preview.tsx`](../../src/components/remotion/remotion-preview.tsx)

---

## Session addendum — Remotion preview: Phase 1 style isolation (no iframe)

### Context

- Implement [`.comms/Remotion Style Isolation Handoff.md`](../../.comms/Remotion%20Style%20Isolation%20Handoff.md): dedicated preview root + scoped reset so host globals and Tailwind preflight do not distort the composition; **no iframe**.

### Work Done

- Added [`src/components/remotion/remotion-preview.css`](../../src/components/remotion/remotion-preview.css): `#remotion-preview-root` scoped rules (`box-sizing`, heading/list/`figure` margin reset, `img`/`svg`/`video`/`canvas` `max-width: none`).
- Updated [`src/components/remotion/remotion-preview.tsx`](../../src/components/remotion/remotion-preview.tsx): import CSS; wrap `<Player />` in `<div id="remotion-preview-root" className={cn("not-prose", "h-full w-full min-h-0")}>`.
- Verification: `npm run typecheck` and ESLint on `remotion-preview.tsx` passed; full `npm run build` compiled then hit unrelated Windows `EPERM` on `.next/trace` in this environment.

### Decisions

- See [`.memory/decisions.md`](../decisions.md) — **2026-04-14 — Remotion preview: host style isolation without iframe**.

### Results

- Preview subtree has a stable isolation boundary and reset layer; handoff Phases 2–3 (extend reset if needed; package Tailwind prefix) remain optional follow-ups.

### Next

- [ ] Optional: DevTools on `/sandbox/interaction-lab/remotion` — confirm scoped rules apply inside the player for `canvas`/`img` as needed.
- [ ] (unchanged) Filters placeholder on Remotion lab page — replace when composition/dataset controls exist.

### Links

- [`.comms/Remotion Style Isolation Handoff.md`](../../.comms/Remotion%20Style%20Isolation%20Handoff.md)
- [`src/components/remotion/remotion-preview.tsx`](../../src/components/remotion/remotion-preview.tsx)
- [`src/components/remotion/remotion-preview.css`](../../src/components/remotion/remotion-preview.css)
