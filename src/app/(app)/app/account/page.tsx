export default function AppAccountPage() {
  return (
    <div className="grid gap-2">
      <h1 className="font-brand text-xl font-semibold">Account</h1>
      <p className="text-muted-foreground text-sm">
        Placeholder only — account settings are out of scope for the initial shell.
      </p>
    </div>
  );
}
