import type { ReactNode } from "react";

export const metadata = {
  title: "Data lab — Assets (list for selection)",
  description:
    "GET /api/assets/list-for-selection — compact published CMS assets for pickers and test harnesses.",
};

export default function DataLabAssetsListForSelectionLayout({ children }: { children: ReactNode }) {
  return children;
}
