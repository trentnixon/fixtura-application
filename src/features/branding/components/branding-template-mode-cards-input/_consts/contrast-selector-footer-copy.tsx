/** Shown under &quot;2. Contrast selector&quot; in the comparison card header. */
export const CONTRAST_SELECTOR_BYLINE_COPY = (
  <>
    Choose the contrast preset that works best for your rendered assets — each pairs title colour
    with container background: <span className="text-foreground/90">Light</span> (white titles,
    white containers), <span className="text-foreground/90">Light Alt</span> (black titles, white
    containers), <span className="text-foreground/90">Dark</span> (black titles, black containers),{" "}
    <span className="text-foreground/90">Dark Alt</span> (white titles, black containers). Saved as
    slug.
  </>
);
