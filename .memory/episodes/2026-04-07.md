# Episode — 2026-04-07

## Context

- Implemented the **account media library** integration: Strapi `GET /api/accounts/:accountId/media-library` exposed via Next BFF, typed client layer, TanStack Query hook, and media gallery UI — aligned with existing account-scoped phases (settings/branding pattern).

## Work Done

- Added BFF route [`src/app/api/accounts/[accountId]/media-library/route.ts`](src/app/api/accounts/[accountId]/media-library/route.ts) (cookie JWT → Strapi passthrough).
- Registered **`appRoutes.accounts.mediaLibrary`** in [`src/lib/api/routes/route-definitions.ts`](src/lib/api/routes/route-definitions.ts).
- Added types `AccountMediaLibraryImage`, `AccountMediaLibraryItem`, `AccountMediaLibraryResponse` in [`src/types/api/account.ts`](src/types/api/account.ts).
- Extended [`src/lib/api/services/account.api.ts`](src/lib/api/services/account.api.ts) with `getAccountMediaLibrary`.
- Added [`queryKeys.account.mediaLibrary`](src/lib/api/query/query-keys.ts) and hook [`useAccountMediaLibrary`](src/lib/api/hooks/account/useAccountMediaLibrary.ts) (gateway redirect pattern for 400/403/404).
- Replaced media gallery placeholder with [`media-gallery-content.tsx`](<src/app/(members)/o/[accountId]/media-gallery/media-gallery-content.tsx>) + async [`page.tsx`](<src/app/(members)/o/[accountId]/media-gallery/page.tsx>) (grid, empty state, loader, errors).

## Decisions

- (none logged — follows established BFF + `accountApi` + hook conventions.)

## Results

- Members route `/o/[accountId]/media-gallery` loads published items from same-origin `/api/accounts/{accountId}/media-library`.
- Typecheck (`tsc --noEmit`) passed for the integration.

## Next

- [ ] Smoke BFF + UI against live Strapi; ensure **Users & permissions → Authenticated → Account → `getAccountMediaLibrary`** is enabled (403 if missing).
- [ ] Optional: single-item detail endpoint ([`app-handoff-get-account-media-library-item-endpoint.md`](<src/app/(members)/o/[accountId]/media-gallery/.comms/app-handoff-get-account-media-library-item-endpoint.md>)) when ready to implement.

## Links

- Handoff: [`src/app/(members)/o/[accountId]/media-gallery/.comms/app-handoff-get-account-media-library-endpoint.md`](<src/app/(members)/o/[accountId]/media-gallery/.comms/app-handoff-get-account-media-library-endpoint.md>)

---

## Update — single-item endpoint + gallery UI split

### Context

- Ship **`GET /api/accounts/:accountId/media-library/:mediaId`** on the app side (BFF + client + hook) per item handoff; surface it in the media gallery without duplicating the first list row when the single fetch succeeds; split gallery UI into `_components` for maintainability.

### Work Done

- Added BFF [`src/app/api/accounts/[accountId]/media-library/[mediaId]/route.ts`](src/app/api/accounts/[accountId]/media-library/[mediaId]/route.ts) (cookie JWT → Strapi; `isValidRenderIdSegment` for `mediaId`).
- Registered **`appRoutes.accounts.mediaLibraryItem`** in [`src/lib/api/routes/route-definitions.ts`](src/lib/api/routes/route-definitions.ts).
- Added type `AccountMediaLibraryItemResponse` in [`src/types/api/account.ts`](src/types/api/account.ts).
- Extended [`src/lib/api/services/account.api.ts`](src/lib/api/services/account.api.ts) with `getAccountMediaLibraryItem`.
- Added [`queryKeys.account.mediaLibraryItem`](src/lib/api/query/query-keys.ts) and hook [`useAccountMediaLibraryItem`](src/lib/api/hooks/account/useAccountMediaLibraryItem.ts) (gateway redirect pattern).
- Updated [`media-gallery-content.tsx`](<src/app/(members)/o/[accountId]/media-gallery/media-gallery-content.tsx>): featured block driven by first list id + single-item query; redirect handling for item query; grid excludes featured row by id when single fetch succeeds.
- Extracted presentational pieces under [`src/app/(members)/o/[accountId]/media-gallery/_components/`](<src/app/(members)/o/[accountId]/media-gallery/_components/>): item card, featured (+ skeleton), grid, empty, redirecting status.

### Decisions

- (none — mirrors existing render-detail / list BFF + hook patterns.)

### Results

- App can load one published row via same-origin BFF `GET /api/accounts/{accountId}/media-library/{mediaId}`; gallery shows a “Featured” card from that endpoint when the account has items.
- Typecheck (`tsc --noEmit`) passed after changes.

### Next

- [ ] Smoke list + single-item BFFs against live Strapi; confirm **`getAccountMediaLibrary`** covers both routes (per handoff).

### Links

- Item handoff: [`src/app/(members)/o/[accountId]/media-gallery/.comms/app-handoff-get-account-media-library-item-endpoint.md`](<src/app/(members)/o/[accountId]/media-gallery/.comms/app-handoff-get-account-media-library-item-endpoint.md>)

---

## Log checkpoint

### Context

- User triggered `log:` again; no further repo changes in this message.

### Work Done

- (none — memory files reviewed; [`state.md`](.memory/state.md) already matches media library list + item + gallery `_components`.)

### Next

- [ ] Unchanged: smoke both media-library BFFs against live Strapi; confirm `getAccountMediaLibrary` permission.

---

## Update — account billing (API + page)

### Context

- Integrate Strapi **`GET /api/accounts/:accountId/billing`** per [`.comms/app-handoff-get-account-billing-endpoint.md`](<src/app/(members)/o/[accountId]/billing/.comms/app-handoff-get-account-billing-endpoint.md>) using the same BFF + `accountApi` + TanStack hook pattern as sponsors; ship `/o/[accountId]/billing` and navigation.

### Work Done

- Registered **`appRoutes.accounts.billing`** in [`src/lib/api/routes/route-definitions.ts`](src/lib/api/routes/route-definitions.ts).
- Added `AccountBillingPayload` / response DTOs in [`src/types/api/account.ts`](src/types/api/account.ts); **`getAccountBilling`** in [`src/lib/api/services/account.api.ts`](src/lib/api/services/account.api.ts).
- Added [`queryKeys.account.billing`](src/lib/api/query/query-keys.ts) and hook [`useAccountBilling`](src/lib/api/hooks/account/useAccountBilling.ts) (gateway redirect for 400/403/404; `staleTime` 60s).
- BFF [`src/app/api/accounts/[accountId]/billing/route.ts`](src/app/api/accounts/[accountId]/billing/route.ts) (cookie JWT → Strapi; `cache: "no-store"`).
- UI: [`billing/page.tsx`](<src/app/(members)/o/[accountId]/billing/page.tsx>) + [`billing-content.tsx`](<src/app/(members)/o/[accountId]/billing/billing-content.tsx>) (summary, financial rollups, orders table, optional tier/trial/customers).
- Nav: [`accountScopedRoutes.billing`](src/lib/config/account-routes.ts); **Billing** in [`app-sidebar.tsx`](src/components/app-sidebar.tsx); title in [`site-header.tsx`](src/components/site-header.tsx); user menu link in [`nav-user.tsx`](src/components/nav-user.tsx) (replaces `/billing`).

### Decisions

- (none — follows sponsors BFF + hook conventions.)

### Results

- Members can open `/o/[accountId]/billing`; app calls same-origin `GET /api/accounts/{accountId}/billing`.
- Typecheck and ESLint on touched new files passed during implementation.

### Next

- [ ] Smoke BFF against live Strapi; enable **Users & permissions → Authenticated → Account → `getAccountBilling`** (403 if missing).

### Links

- Handoff: [`src/app/(members)/o/[accountId]/billing/.comms/app-handoff-get-account-billing-endpoint.md`](<src/app/(members)/o/[accountId]/billing/.comms/app-handoff-get-account-billing-endpoint.md>)

---

## Update — account sponsors (GET + manage-sponsors UI)

### Context

- Integrate Strapi **`GET /api/accounts/:accountId/sponsors`** per [`app-handoff-get-account-sponsors-endpoint.md`](<src/app/(members)/o/[accountId]/manage-sponsors/.comms/app-handoff-get-account-sponsors-endpoint.md>): BFF, typed client, TanStack hook, read-only list UI on `/o/[accountId]/manage-sponsors` (create/edit deferred).

### Work Done

- Registered **`appRoutes.accounts.sponsors`** in [`src/lib/api/routes/route-definitions.ts`](src/lib/api/routes/route-definitions.ts).
- Added `AccountSponsorshipAllocation`, `AccountSponsorDto`, `AccountSponsorsData`, `AccountSponsorsResponse` in [`src/types/api/account.ts`](src/types/api/account.ts) (logo reuses `BrandingMedia`).
- Extended [`src/lib/api/services/account.api.ts`](src/lib/api/services/account.api.ts) with **`getAccountSponsors`**.
- Added [`queryKeys.account.sponsors`](src/lib/api/query/query-keys.ts) and hook [`useAccountSponsors`](src/lib/api/hooks/account/useAccountSponsors.ts) (gateway redirect for 400/403/404).
- BFF [`src/app/api/accounts/[accountId]/sponsors/route.ts`](src/app/api/accounts/[accountId]/sponsors/route.ts) (cookie JWT → Strapi; `cache: "no-store"`).
- UI: [`manage-sponsors/page.tsx`](<src/app/(members)/o/[accountId]/manage-sponsors/page.tsx>) + [`manage-sponsors-content.tsx`](<src/app/(members)/o/[accountId]/manage-sponsors/manage-sponsors-content.tsx>) (cards: logo, dates, badges, URL, allocations as JSON).

### Decisions

- (none — follows existing account-scoped BFF + hook pattern.)

### Results

- Members can open `/o/[accountId]/manage-sponsors`; app calls same-origin `GET /api/accounts/{accountId}/sponsors`.
- Typecheck (`tsc --noEmit`) passed during implementation.

### Next

- [ ] Smoke BFF against live Strapi; enable **Users & permissions → Authenticated → Account → `getAccountSponsors`** (403 if missing).

### Links

- Handoff: [`src/app/(members)/o/[accountId]/manage-sponsors/.comms/app-handoff-get-account-sponsors-endpoint.md`](<src/app/(members)/o/[accountId]/manage-sponsors/.comms/app-handoff-get-account-sponsors-endpoint.md>)

---

## Update — onboarding PDR (`handoff-onboarding.md`)

### Context

- User requested `LOG:` after iterative updates to the members onboarding product/design handoff: three-part structure (product / FE architecture / codebase alignment), then **Part 4 — Implementation strategy** (contract-first delivery order).

### Work Done

- **Part 4 — Implementation strategy** added to [`src/app/(members)/.comms/handoff-onboarding.md`](<src/app/(members)/.comms/handoff-onboarding.md>): contract-first order (data discovery → CMS/API contract → backend implementation → app data layer → UI last); phases 1–5 with rationale; final recommendation (backend first, protocol second, UI last; step-scoped persistence; setup status as server state).
- Document intro updated to **four layers**; Part 1 overview cross-references Part 4 for delivery order; **Definition of success** promoted to a top-level `#` section after Part 4.

### Decisions

- (none — documentation only.)

### Results

- Single handoff doc now covers product/UX, FE patterns, repo alignment/gaps/open questions, and **delivery sequencing** for onboarding.

### Next

- [ ] Execute Part 4 order with CMS: data inventory → API contracts → BFF/app layer → onboarding UI (no UI-first against assumptions).

### Links

- [`src/app/(members)/.comms/handoff-onboarding.md`](<src/app/(members)/.comms/handoff-onboarding.md>)

---

## Update — CMS onboarding API handoff + create-organisation phased plan

### Context

- **Contract-first onboarding:** Document CMS/backend requirements and a **phased FE integration path** for the `create-organisation` route (wizard shell → A1 → steps + writes → setup status → redirect), without implementing BFF mutations until OpenAPI exists.

### Work Done

- Added [`src/app/(members)/.comms/cms-handoff-onboarding-api-requirements.md`](<src/app/(members)/.comms/cms-handoff-onboarding-api-requirements.md>): BFF vs CMS source of truth (§2.1), W4/draft/permission/M1+W2/R1 minimum GETs/S1 retryable clarifications, **§9** review gap table + workshop Q&A (product / CMS / semantics / BFF / process).
- Added feature planning under [`src/app/(members)/create-organisation/.docs/`](<src/app/(members)/create-organisation/.docs/>): [`readMe.md`](<src/app/(members)/create-organisation/.docs/readMe.md>), [`DevelopmentRoadMap.md`](<src/app/(members)/create-organisation/.docs/DevelopmentRoadMap.md>), [`PhasedIntegrationPath.md`](<src/app/(members)/create-organisation/.docs/PhasedIntegrationPath.md>) (wizard table + **Phase 0–7** with exists/build/endpoints), [`Tickets.md`](<src/app/(members)/create-organisation/.docs/Tickets.md>) (**TKT-2026-ONB-001**–**008**), [`Completed.md`](<src/app/(members)/create-organisation/.docs/Completed.md>); [`../.comms/`](<src/app/(members)/create-organisation/.comms/>) and [`../.research/`](<src/app/(members)/create-organisation/.research/>) placeholders.

### Decisions

- (none — documentation and planning only; contract-first decision already in [`decisions.md`](.memory/decisions.md) 2026-04-07 entry.)

### Results

- CMS and product have a single **requirements + gaps** handoff with answered review items where possible; FE has a **step-by-step** integration checklist tied to capability IDs **A1, L1–L3, W1–W4, S1/S2, R1, M1**.

### Next

- [ ] Workshop: data matrix, A1 lifecycle, then OpenAPI; then **Phase 0** shell in [`create-organisation/page.tsx`](<src/app/(members)/create-organisation/page.tsx>) per `PhasedIntegrationPath.md`.

### Links

- [`cms-handoff-onboarding-api-requirements.md`](<src/app/(members)/.comms/cms-handoff-onboarding-api-requirements.md>)
- [`PhasedIntegrationPath.md`](<src/app/(members)/create-organisation/.docs/PhasedIntegrationPath.md>)

---

## Update — Phase 3 closed in docs (testing owns CMS E2E)

### Context

- User confirmed **Phase 3** (Step 2 branding, TKT-2026-ONB-004) is **done** for app delivery; remaining Strapi/CMS verification to be handled in **testing/QA**, and requested **`LOG:`**.

### Work Done

- Updated [`create-organisation/.docs/Tickets.md`](<src/app/(members)/create-organisation/.docs/Tickets.md>) (TKT-004 completion summary: Phase 3 closed for delivery; E2E in testing).
- Updated [`create-organisation/.docs/Completed.md`](<src/app/(members)/create-organisation/.docs/Completed.md>), [`PhasedIntegrationPath.md`](<src/app/(members)/create-organisation/.docs/PhasedIntegrationPath.md>) (status + document history), [`DevelopmentRoadMap.md`](<src/app/(members)/create-organisation/.docs/DevelopmentRoadMap.md>) (Phase 3 bullet).

### Decisions

- Logged in [`.memory/decisions.md`](.memory/decisions.md): Phase 3 app scope closed; CMS E2E in QA.

### Results

- Planning docs align with **Phase 4 (W3)** as current focus; Phase 3 explicitly closed pending QA.

### Next

- [ ] Implement **Phase 4** — Step 3 contact (**W3**), TKT-2026-ONB-005.
- [ ] QA: smoke onboarding Step 2 + branding against live Strapi per handoffs.

### Links

- [`Tickets.md`](<src/app/(members)/create-organisation/.docs/Tickets.md>)
- [`PhasedIntegrationPath.md`](<src/app/(members)/create-organisation/.docs/PhasedIntegrationPath.md>)

---

## Update — onboarding Phase 4 (W3 contact / delivery)

### Context

- Ship **Phase 4** — Step 3 operational contact (**W3**): BFF proxy, client layer, wizard UI, and CMS-facing docs; then integrate **CMS team response** (Strapi route live, permissions, validation limits, no **409** in v1).

### Work Done

- BFF [`src/app/api/accounts/[accountId]/onboarding/step-3/route.ts`](src/app/api/accounts/[accountId]/onboarding/step-3/route.ts); registry **`accounts.onboardingStep3`**; types `UpdateOnboardingStep3Body` / response in [`src/types/api/account.ts`](src/types/api/account.ts); **`accountApi.updateOnboardingStep3`**; hook [`useUpdateOnboardingStep3`](src/lib/api/hooks/account/useUpdateOnboardingStep3.ts).
- UI: [`wizard-step-contact.tsx`](<src/app/(members)/create-organisation/_components/wizard-step-contact.tsx>) + [`create-organisation-wizard.tsx`](<src/app/(members)/create-organisation/_components/create-organisation-wizard.tsx>) step 3 wiring; **maxLength** 255 / 255 / 4000 per CMS.
- Comms: [`app-handoff-onboarding-phase4-w3.md`](<src/app/(members)/create-organisation/.comms/phase-4/app-handoff-onboarding-phase4-w3.md>), [`cms-request-phase4-w3-contact-and-open-questions.md`](<src/app/(members)/create-organisation/.comms/phase-4/cms-request-phase4-w3-contact-and-open-questions.md>), [`cms-response-phase4-w3-contact-and-delivery.md`](<src/app/(members)/create-organisation/.comms/phase-4/cms-response-phase4-w3-contact-and-delivery.md>), [`integration-notes-phase4-step3.md`](<src/app/(members)/create-organisation/.comms/phase-4/integration-notes-phase4-step3.md>).
- Planning: TKT-2026-ONB-005 **Completed** in [`Tickets.md`](<src/app/(members)/create-organisation/.docs/Tickets.md>); [`Completed.md`](<src/app/(members)/create-organisation/.docs/Completed.md>), [`DevelopmentRoadMap.md`](<src/app/(members)/create-organisation/.docs/DevelopmentRoadMap.md>), [`PhasedIntegrationPath.md`](<src/app/(members)/create-organisation/.docs/PhasedIntegrationPath.md>), [`readMe.md`](<src/app/(members)/create-organisation/.docs/readMe.md>) updated for Phase 4 + CMS reply.

### Decisions

- Logged in [`.memory/decisions.md`](.memory/decisions.md): Phase 4 W3 app scope closed; ops/QA for Strapi.

### Results

- Wizard Step 3 persists contact fields via same-origin BFF; docs record CMS answers (permission id, validation, **409** deferred).

### Next

- [ ] Enable Strapi **`updateOnboardingStep3`**; smoke **DeliveryAddress** migration + PATCH → GET settings.
- [ ] **Phase 5** — review + **W4** (TKT-2026-ONB-006).

### Links

- CMS response: [`cms-response-phase4-w3-contact-and-delivery.md`](<src/app/(members)/create-organisation/.comms/phase-4/cms-response-phase4-w3-contact-and-delivery.md>)

---

## Update — image crop uploader: Sonner + interaction-lab UX

### Context

- Polish **shared** `ImageUploaderCrop` / **interaction-lab** `upload/image-crop` page: validation feedback pattern, crop-ratio UX, debug presentation.

### Work Done

- **Validation notifications:** Pre-crop (dropzone reject + `validateImageFile`), post-crop (`validateCroppedOutput`), and crop pipeline errors use **`toast.error`** (Sonner); successful crop uses **`toast.success`** with dimensions in the description. Removed inline destructive copy under the uploader and removed **`validationError`** from **`ImageCropDialog`** (no red strip above footer).
- **`hideAspectPresetOnUploader`:** When multiple **`aspectPresets`** exist, ratio selection can be **dialog-only** (lab “Choose crop ratio” card uses this).
- **Lab page:** “Choose crop ratio” card — gradient accent + ring retained; layout simplified to **CardHeader + CardContent** (not kitchen-sink split); copy/helper text updated; **`pb-4`** under drop zone; **debug** panels use **Variant B** tinted info-style cards (`DebugOutputTintedCard`).
- **Lint:** e.g. `bg-primary/6` where suggested.

### Decisions

- Logged in [`.memory/decisions.md`](.memory/decisions.md): Sonner for image validation/success on shared uploader.

### Results

- `ImageUploaderCrop` aligns with global **Toaster** in root layout; `onError` callbacks unchanged for parents.

### Next

- [ ] When embedding `ImageUploaderCrop` in product flows (e.g. onboarding branding), rely on toasts + optional `onError` for analytics or secondary UI.

### Links

- [`src/components/media/image-uploader-crop.tsx`](src/components/media/image-uploader-crop.tsx)
- [`src/components/media/image-crop-dialog.tsx`](src/components/media/image-crop-dialog.tsx)
- [`src/app/sandbox/interaction-lab/upload/image-crop/page.tsx`](src/app/sandbox/interaction-lab/upload/image-crop/page.tsx)

---

## Update — Phase 0 documented in `PhasedIntegrationPath` (done + deliverables)

### Context

- User requested **`LOG:`** after updating onboarding docs so **Phase 0** is clearly marked **complete** and the **deliverables** are recorded for product/CMS review.

### Work Done

- Updated [`src/app/(members)/create-organisation/.docs/PhasedIntegrationPath.md`](<src/app/(members)/create-organisation/.docs/PhasedIntegrationPath.md>): **What exists today** lists `page.tsx`, `_components/create-organisation-wizard.tsx`, and behaviour summary; **Phase 0** section set to **Status: Done (TKT-2026-ONB-001)** with a **Delivered** table (route, UI shell, state, escape hatches, no API), exit criteria marked **met**; **Document history** row added.
- Updated [`DevelopmentRoadMap.md`](<src/app/(members)/create-organisation/.docs/DevelopmentRoadMap.md>): **Completed** bullet expanded with links to `PhasedIntegrationPath.md` and `Completed.md`.
- Updated [`Completed.md`](<src/app/(members)/create-organisation/.docs/Completed.md>): TKT-2026-ONB-001 summary expanded (files, UX notes, link to phased doc).

### Decisions

- (none — documentation only.)

### Results

- Phased integration doc matches the shipped Phase 0 shell; roadmap and completed archive stay cross-linked.

### Next

- [ ] Unchanged: **Phase 5** — review + **W4** (TKT-2026-ONB-006); see [`.memory/state.md`](.memory/state.md).

### Links

- [`PhasedIntegrationPath.md`](<src/app/(members)/create-organisation/.docs/PhasedIntegrationPath.md>)
- [`Completed.md`](<src/app/(members)/create-organisation/.docs/Completed.md>)

---

## Update — Phase 1 A1 (first account) + handoff hygiene

### Context

- Session work: ship **A1** in the members app (BFF proxy, route registry, `accountApi.createFirstAccount`, `useCreateFirstAccount`, wizard **Get Started** when bootstrap has no account rows); align **comms** and BFF with CMS-locked behaviour (**200** vs **201**); answer **Phase 2 readiness** and **Phase 1 testing** expectations.

### Work Done

- **BFF:** [`src/app/api/account/first/route.ts`](src/app/api/account/first/route.ts) — `POST /api/account/first` → Strapi `POST …/api/account/first`; forward JSON body; pass through **success** HTTP status from Strapi (**200** / **201**); errors passthrough.
- **Client layer:** [`appRoutes.account.first`](src/lib/api/routes/route-definitions.ts); [`CreateFirstAccountResponse`](src/types/api/account.ts); [`accountApi.createFirstAccount`](src/lib/api/services/account.api.ts); [`useCreateFirstAccount`](src/lib/api/hooks/account/useCreateFirstAccount.ts) invalidates `queryKeys.account.me` on success.
- **UI:** [`create-organisation-wizard.tsx`](<src/app/(members)/create-organisation/_components/create-organisation-wizard.tsx>) — Get Started runs A1 when `accountPickerRowsFromMePayload` is empty; loading/error affordances.
- **Comms:** [`app-handoff-post-account-first-endpoint.md`](<src/app/(members)/create-organisation/.comms/app-handoff-post-account-first-endpoint.md>) — fixed links (`get-account-me`, `PhasedIntegrationPath`), clarified **Strapi** vs Next repo paths, CMS delivery header; planning docs updated for Phase 1 completion (see repo `Tickets.md` / `PhasedIntegrationPath.md` as of that change set).

### Decisions

- (none — follows existing BFF + TanStack patterns.)

### Results

- Zero-account users can trigger first-account creation from the wizard when upstream is deployed; bootstrap refresh via `account.me` invalidation.

### Next

- [ ] QA: enable Strapi **`api::account.account.createFirstAccount`**; smoke **POST /api/account/first** → **GET /api/account/me** for zero-account users.
- [ ] **Phase 2** (L1–L3, W1) blocked on CMS OpenAPI + product data matrix — see chat / [`PhasedIntegrationPath.md`](<src/app/(members)/create-organisation/.docs/PhasedIntegrationPath.md>).

### Links

- [`app-handoff-post-account-first-endpoint.md`](<src/app/(members)/create-organisation/.comms/app-handoff-post-account-first-endpoint.md>)

---

## Update — Phase 2 onboarding (L1/L2, associations, clubs, W1) + CMS sign-off docs

### Context

- **`LOG:`** after shipping **Phase 2** for **`/create-organisation`**: sport / org-type / association / club lookups, **W1** `PATCH` Step 1, wizard UI aligned with CMS handoffs; backend Q&A captured (e.g. **`account_type.id === 1`** = club).

### Work Done

- **HTTP client:** `PATCH` on **`HttpMethod`**; **`apiClient.patch`** for step-scoped writes.
- **BFF:** [`src/app/api/account/onboarding/lookups/`](src/app/api/account/onboarding/lookups/) — `sports`, `organisation-types`, `associations` (`?sport=`), `clubs` (`?associationId=`); [`src/app/api/accounts/[accountId]/onboarding/step-1/route.ts`](src/app/api/accounts/[accountId]/onboarding/step-1/route.ts) for **W1**.
- **Client:** [`route-definitions.ts`](src/lib/api/routes/route-definitions.ts), [`account.api.ts`](src/lib/api/services/account.api.ts), [`query-keys.ts`](src/lib/api/query/query-keys.ts); hooks for lookups + **`useUpdateOnboardingStep1`** (invalidates **`account.me`**, settings, organisation context).
- **UI:** [`wizard-step-organisation.tsx`](<src/app/(members)/create-organisation/_components/wizard-step-organisation.tsx>) — sport → org type → association → optional club when **`accountTypeId === CLUB_ACCOUNT_TYPE_ID`**; **`onboardingOrganisationName`** derived from club vs association label (no free-text org name); read-only preview; permission checkboxes; controls disabled until sport selected. **`create-organisation-wizard.tsx`** — Step 1 wired with ref submit + **`accountId`** from bootstrap. Config: [`src/lib/config/onboarding.ts`](src/lib/config/onboarding.ts) (**`CLUB_ACCOUNT_TYPE_ID = 1`**).
- **Types:** **`UpdateOnboardingStep1Body`** includes **`associationId`**, **`clubId`**; settings/summary types updated as needed for **`onboardingOrganisationName`**.
- **Comms:** canonical CMS→app handoff ([`app-handoff-onboarding-associations-clubs.md`](<src/app/(members)/create-organisation/.comms/app-handoff-onboarding-associations-clubs.md>)), backend sign-off ([`cms-phase2-backend-signoff.md`](<src/app/(members)/create-organisation/.comms/cms-phase2-backend-signoff.md>)), cross-links from request docs.

### Decisions

- Logged in [`.memory/decisions.md`](.memory/decisions.md): **club** org type identified by **`account_type.id === 1`** (not label regex).

### Results

- App-side **Phase 2** flow is implementable against deployed Strapi; **“complete”** for product still requires **live routes + permissions + QA** (JWT, GET lookups, PATCH W1, **`account.me`** / settings).

### Next

- [ ] **Strapi / ops:** Deploy matching lookup + W1 routes; **Users & permissions → Authenticated → Account** for required actions; **E2E smoke** (lookups + PATCH W1 + derived name).
- [ ] **FE roadmap:** Continue **Phase 5** (review + **W4**) per [`PhasedIntegrationPath.md`](<src/app/(members)/create-organisation/.docs/PhasedIntegrationPath.md>) and [`.memory/state.md`](.memory/state.md).

### Links

- [`app-handoff-onboarding-associations-clubs.md`](<src/app/(members)/create-organisation/.comms/app-handoff-onboarding-associations-clubs.md>)
- [`cms-phase2-backend-signoff.md`](<src/app/(members)/create-organisation/.comms/cms-phase2-backend-signoff.md>)

---

## Update — `LOG:`: state synced to Phases 0–7 complete

### Context

- User triggered **`LOG:`** while `PhasedIntegrationPath.md` / `DevelopmentRoadMap.md` document **Phase 5 (W4)**, **Phase 6 (S1/S2)**, and **Phase 7 (redirect)** as **Done**; [`.memory/state.md`](.memory/state.md) still described **Phase 5** as next.

### Work Done

- Rewrote [`.memory/state.md`](.memory/state.md): **current focus** = onboarding wizard **through Phase 7** (TKT-2026-ONB-001–008); **follow-ups** = middleware §5 + gateway Q1 (per roadmap); **next actions** = QA E2E vs Strapi, product/CMS semantics, temp dumps; **blockers** = upstream Strapi handlers for first account, confirm, setup-status, and related step routes.
- Appended this episode section (no new `decisions.md` entry — operational sync only).

### Decisions

- (none)

### Results

- Memory **state** matches [`DevelopmentRoadMap.md`](<src/app/(members)/create-organisation/.docs/DevelopmentRoadMap.md>) and phased path (Phases **5–7** marked done in docs; Strapi still pending for several routes).

### Next

- [ ] Unchanged: live Strapi smoke and permissions per roadmap _Blocked / Waiting_.

### Links

- [`PhasedIntegrationPath.md`](<src/app/(members)/create-organisation/.docs/PhasedIntegrationPath.md>)
- [`DevelopmentRoadMap.md`](<src/app/(members)/create-organisation/.docs/DevelopmentRoadMap.md>)

---

## Log checkpoint

### Context

- User triggered **`LOG:`** again; no new code or doc edits in this message.

### Work Done

- Confirmed [`.memory/state.md`](.memory/state.md) still matches onboarding **Phases 0–7** shipped in app; follow-ups remain **QA vs Strapi**, **§5** / gateway **Q1**, and replacing temp dumps.

### Decisions

- (none)

### Results

- No change to `state.md` or `decisions.md` this turn.

### Next

- [ ] Unchanged: see [`.memory/state.md`](.memory/state.md) — **Next actions** and **Blocked / Waiting**.

### Links

- (none)

---

## Update — Phase 6 S1/S2 app delivery + CMS comms sync

### Context

- **Phase 6 (setup status):** Ship **S1** BFF + client polling + `SetupStatusCard`; document **S2**; close **TKT-2026-ONB-007** in feature docs.
- **CMS team:** Tighten deploy permission doc, canonical phase-6 handoff, remove duplicate markdown, cross-link **§4.4** in `cms-handoff-onboarding-api-requirements.md`.

### Work Done

- **App:** BFF [`src/app/api/accounts/[accountId]/onboarding/setup-status/route.ts`](src/app/api/accounts/[accountId]/onboarding/setup-status/route.ts); `accounts.onboardingSetupStatus` in [`route-definitions.ts`](src/lib/api/routes/route-definitions.ts); [`accountApi.getOnboardingSetupStatus`](src/lib/api/services/account.api.ts); [`parseOnboardingSetupStatusPayload`](src/lib/api/parse-onboarding-setup-status.ts); [`useOnboardingSetupStatus`](src/lib/api/hooks/account/useOnboardingSetupStatus.ts) with terminal-aware polling ([`onboarding.ts`](src/lib/config/onboarding.ts)); [`queryKeys.account.setupStatus`](src/lib/api/query/query-keys.ts); invalidation from [`useUpdateOnboardingStep1`](src/lib/api/hooks/account/useUpdateOnboardingStep1.ts) + [`useConfirmOnboarding`](src/lib/api/hooks/account/useConfirmOnboarding.ts); UI [`setup-status-card.tsx`](<src/app/(members)/create-organisation/_components/setup-status-card.tsx>) on [`wizard-step-organisation.tsx`](<src/app/(members)/create-organisation/_components/wizard-step-organisation.tsx>) + [`wizard-step-review.tsx`](<src/app/(members)/create-organisation/_components/wizard-step-review.tsx>).
- **Feature docs:** [`PhasedIntegrationPath.md`](<src/app/(members)/create-organisation/.docs/PhasedIntegrationPath.md>), [`Tickets.md`](<src/app/(members)/create-organisation/.docs/Tickets.md>), [`Completed.md`](<src/app/(members)/create-organisation/.docs/Completed.md>), [`DevelopmentRoadMap.md`](<src/app/(members)/create-organisation/.docs/DevelopmentRoadMap.md>), [`readMe.md`](<src/app/(members)/create-organisation/.docs/readMe.md>) — Phase 6 **Done**; **TKT-2026-ONB-007** completed.
- **Comms:** [`app-handoff-onboarding-phase6-s1-s2.md`](<src/app/(members)/create-organisation/.comms/phase-6/app-handoff-onboarding-phase6-s1-s2.md>) (deploy cross-link, fixed `../cms-handoff-onboarding-api-requirements.md`); [`deploy-get-onboarding-setup-status-permission.md`](<src/app/(members)/create-organisation/.comms/phase-6/deploy-get-onboarding-setup-status-permission.md>) (`getOnboardingSetupStatus`, Strapi path `getOnboardingSetupStatusPayload` as CMS codebase note); removed duplicate `app-handoff-onboarding-phase6-s1-s2 copy.md`; [`cms-handoff-onboarding-api-requirements.md`](<src/app/(members)/create-organisation/.comms/cms-handoff-onboarding-api-requirements.md>) §4.4 **Strapi implementation** paragraph with links.

### Decisions

- (none — follows existing BFF + TanStack patterns; Strapi owns upstream GET + permission.)

### Results

- Members app ready to poll **S1** when Strapi exposes `GET …/onboarding/setup-status` and grants **`getOnboardingSetupStatus`**; UI degrades gracefully on **404** until then.

### Next

- [ ] Strapi: deploy GET + enable **`getOnboardingSetupStatus`**; smoke BFF + wizard setup card ([`deploy-get-onboarding-setup-status-permission.md`](<src/app/(members)/create-organisation/.comms/phase-6/deploy-get-onboarding-setup-status-permission.md>)).

### Links

- Phase 6 handoff: [`app-handoff-onboarding-phase6-s1-s2.md`](<src/app/(members)/create-organisation/.comms/phase-6/app-handoff-onboarding-phase6-s1-s2.md>)

---

## Update — Phase 5 W4 (review + confirm) + handoff deploy notes

### Context

- Complete onboarding **Phase 5** in the members app: Step 4 read-only review (parallel GETs per §4.5; **R1** deferred), **W4** `POST …/onboarding/confirm`, TanStack invalidations, and CMS-facing handoff including **Strapi** `confirmOnboarding` permission after deploy.

### Work Done

- **BFF:** [`src/app/api/accounts/[accountId]/onboarding/confirm/route.ts`](src/app/api/accounts/[accountId]/onboarding/confirm/route.ts) — `POST` → Strapi `…/onboarding/confirm`; JSON body passthrough; same auth/`isValidAccountIdSegment` pattern as step-3.
- **Client:** `accounts.onboardingConfirm` in [`route-definitions.ts`](src/lib/api/routes/route-definitions.ts); `ConfirmOnboardingResponse` + **`accountApi.confirmOnboarding`** in [`account.ts`](src/types/api/account.ts) / [`account.api.ts`](src/lib/api/services/account.api.ts); [`useConfirmOnboarding`](src/lib/api/hooks/account/useConfirmOnboarding.ts) — `onSuccess` invalidates `account.me`, `settings`, `organisation-context`, `branding`, `auth.me`.
- **UI:** [`wizard-step-review.tsx`](<src/app/(members)/create-organisation/_components/wizard-step-review.tsx>) — parallel queries, partial failure + retry, summary sections, **409** handling; [`create-organisation-wizard.tsx`](<src/app/(members)/create-organisation/_components/create-organisation-wizard.tsx>) — step 4 + **Finish** + `wizardCompleted` success state.
- **Docs:** TKT-2026-ONB-006 **Completed**; [`PhasedIntegrationPath.md`](<src/app/(members)/create-organisation/.docs/PhasedIntegrationPath.md>), [`Tickets.md`](<src/app/(members)/create-organisation/.docs/Tickets.md>), [`Completed.md`](<src/app/(members)/create-organisation/.docs/Completed.md>), [`DevelopmentRoadMap.md`](<src/app/(members)/create-organisation/.docs/DevelopmentRoadMap.md>), [`readMe.md`](<src/app/(members)/create-organisation/.docs/readMe.md>).
- **Handoff:** [`app-handoff-onboarding-phase5-w4.md`](<src/app/(members)/create-organisation/.comms/phase-5/app-handoff-onboarding-phase5-w4.md>) — members app implementation table, **Deploy** checklist (Authenticated → Account → **`confirmOnboarding`**), **403** when missing.

### Decisions

- (none — follows W1–W3 BFF + mutation patterns; R1 aggregate deferred.)

### Results

- Wizard Step 4 can call same-origin **W4** confirm; docs record Strapi permission requirement for **403** avoidance.

### Next

- [ ] Strapi: deploy `POST /api/accounts/:accountId/onboarding/confirm` + enable **`confirmOnboarding`** on Authenticated; smoke confirm after steps 1–3.
- [ ] Continue roadmap QA per [`.memory/state.md`](.memory/state.md).

### Links

- [`app-handoff-onboarding-phase5-w4.md`](<src/app/(members)/create-organisation/.comms/phase-5/app-handoff-onboarding-phase5-w4.md>)
