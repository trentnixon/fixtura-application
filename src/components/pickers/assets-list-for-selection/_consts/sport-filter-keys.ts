/** Sport filter: show every sport (no narrowing). */
export const ALL_SPORTS_KEY = "__all__";

export const NO_SPORT_KEY = "__no_sport__";

/** Default sport filter when the picker mounts (matches API `Sport` enum). */
export const DEFAULT_SPORT_FILTER_KEY = "Cricket" as const;
