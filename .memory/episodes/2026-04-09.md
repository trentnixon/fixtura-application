# Episode — 2026-04-09

## Context

- Close the **Epic 4: Centralize Lifecycle Route Decisions** integration plan (CMS dependency, backlog alignment, no duplicate lifecycle routing).
- User triggered **`LOG:`** to record work in repo memory.

## Work Done

- **Epic 4 / CMS:** Confirmed **no new Strapi endpoints** are required for Epic 4 — routing derives from existing **GET** `onboarding-state` (`OnboardingStateData`) via [`resolve-account-entry.ts`](src/lib/onboarding/resolve-account-entry.ts).
- **App:** [`setup-client.tsx`](<src/app/(members)/create-organisation/setup/setup-client.tsx>) — dashboard / loader / invalidate branches now use `resolveAccountEntry(...) === "dashboard"` instead of bare `isSetup`, matching wizard redirect and shared lifecycle rules.
- **Backlog:** [`.comms/CODEX/ONBOARDING_IMPLEMENTATION_BACKLOG.md`](.comms/CODEX/ONBOARDING_IMPLEMENTATION_BACKLOG.md) — Epic 4 **Completed** with completion note and ticket **4.1** / **4.2** marked done; **Backlog By Team** and **Recommended Sequence** adjusted; **Epic 5** gained a **Scope note (CMS / BFF vs Epic 4)** (payload correctness under **5.1**, not Epic 4).
- **Verification:** `vitest` — `resolve-account-entry.test.ts`, `epic1-lifecycle-gate.test.ts` (17 tests passed).

## Decisions

- (none — backlog and code align with existing resolver design.)

## Results

- Epic 4 is **documented and implemented** in app + backlog; BFF/CMS contract verification remains under **Epic 5**.
- Preparation gateway uses the **same resolver** for all lifecycle branches from onboarding-state.

## Next

- [ ] **Epic 5.1:** BFF alignment checks for onboarding-state / setup-status / retry-setup proxies.
- [ ] **QA:** E2E onboarding vs Strapi (existing state.md items).

## Links

- [`ONBOARDING_IMPLEMENTATION_BACKLOG.md`](.comms/CODEX/ONBOARDING_IMPLEMENTATION_BACKLOG.md) (Epic 4, Epic 5 scope note)
- [`resolve-account-entry.ts`](src/lib/onboarding/resolve-account-entry.ts)
- [`setup-client.tsx`](<src/app/(members)/create-organisation/setup/setup-client.tsx>)

---

## Addendum — `LOG:` Epic 5 BFF contract completion

### Context

- Implement Epic 5 plan: shared Strapi→BFF response mapping, tests, verification comms, Ticket 5.2 delete-account placeholder (Epic 6 dependency).

### Work Done

- **BFF:** Added [`nextResponseFromStrapiFetch`](src/lib/api/bff/next-response-from-strapi-fetch.ts); refactored GET `onboarding-state`, `setup-status`, and POST `retry-setup` routes to use it.
- **Tests:** [`next-response-from-strapi-fetch.test.ts`](src/lib/api/bff/next-response-from-strapi-fetch.test.ts) — JSON pass-through, error shapes, edge cases.
- **Comms:** [`epic-5-bff-contract-verification.md`](<src/app/(members)/create-organisation/.comms/epic-5-bff-contract-verification.md>) (parity matrix, BFF-only errors); [`epic-5-ticket-5-2-delete-account-bff-placeholder.md`](<src/app/(members)/create-organisation/.comms/epic-5-ticket-5-2-delete-account-bff-placeholder.md>) (invalidations, Epic 6 link).

### Decisions

- (none — contract preservation via single helper; delete-account remains gated on Epic 6.1.)

### Results

- Automated coverage for BFF pass-through semantics; docs support staging QA and Epic 6 handoff.

### Next

- [ ] Optional staging: compare BFF vs direct Strapi for the three endpoints (verification doc checklist).
- [ ] Epic 6.1: finalize delete-account contract before BFF implementation.

---

## Addendum — `LOG:` Post-delete onboarding-state 404 (React Query)

### Context

- After **DELETE** unfinished account, the wizard still mounted with the same `accountId` briefly; **`invalidateQueries`** on onboarding-state triggered a **refetch** → **404** / `ApiError` (“Account not found”).

### Work Done

- **[`useDeleteUnfinishedAccount.ts`](src/lib/api/hooks/account/useDeleteUnfinishedAccount.ts):** `cancelQueries` for onboarding-state at start of `onSuccess`; account-scoped keys (`onboardingState`, `setupStatus`, `settings`, `organisationContext`, `branding`) use **`invalidateQueries` + `refetchType: 'none'`**; `account.me` and `auth.me` keep default invalidation; then `router.replace` to organisation selection.
- **Tests:** [`useDeleteUnfinishedAccount.test.tsx`](src/lib/api/hooks/account/useDeleteUnfinishedAccount.test.tsx) — expectations for `cancelQueries` and `refetchType: 'none'` on scoped keys.

### Decisions

- (none — cache semantics only; no API contract change.)

### Results

- No automatic refetch for deleted account keys while the create-organisation wizard is still subscribed; avoids spurious **GET …/onboarding/onboarding-state** after successful delete.

### Next

- [ ] Optional manual QA: delete unfinished account → land on select-organisation without error overlay.

---

## Addendum — `LOG:` Epic 7 QA verification (docs + OrgAccessBoundary tests)

### Context

- Implement Epic 7 plan: repeatable QA checklists for tickets 7.1–7.3, consolidated sign-off doc, optional automated coverage for scoped-route lifecycle gating.

### Work Done

- **Comms (`.comms/CODEX/`):** [`EPIC_7_TICKET_7_1_FRONTEND_ROUTE_QA.md`](.comms/CODEX/EPIC_7_TICKET_7_1_FRONTEND_ROUTE_QA.md) (select-org, deep link, wizard resume, prep→dashboard); [`EPIC_7_TICKET_7_2_CMS_LIFECYCLE_QA.md`](.comms/CODEX/EPIC_7_TICKET_7_2_CMS_LIFECYCLE_QA.md) (CMS transitions + worker/payload alignment); [`EPIC_7_TICKET_7_3_RECOVERY_QA.md`](.comms/CODEX/EPIC_7_TICKET_7_3_RECOVERY_QA.md) (retry, delete, blocked paths); [`EPIC_7_QA_SIGNOFF.md`](.comms/CODEX/EPIC_7_QA_SIGNOFF.md) (Pass/Fail tables + Definition of Done cross-check).
- **Tests:** [`org-access-boundary.test.tsx`](src/components/auth/org-access-boundary.test.tsx) — redirect to `accountEntryFromOnboardingState` when not dashboard-ready; children when `isSetup === true`; invalid segment → select-org `invalid_org`.

### Decisions

- (none — QA process and regression tests only.)

### Results

- Team can run manual matrices per environment and record sign-off in one place; CI covers `OrgAccessBoundary` redirect behaviour aligned with [`resolve-account-entry.ts`](src/lib/onboarding/resolve-account-entry.ts).

### Next

- [ ] Run 7.1 / 7.2 / 7.3 checklists in staging (minimum) and complete [`EPIC_7_QA_SIGNOFF.md`](.comms/CODEX/EPIC_7_QA_SIGNOFF.md).

---

## Addendum — `LOG:` Select-organisation lifecycle card tones + initials

### Context

- Product request: `/select-organisation` cards reflect onboarding lifecycle — **error** (wizard incomplete), **warning** (wizard done, setup pending), **default** (complete); initials when no logo; optional broken-image fallback.

### Work Done

- **Helpers:** [`select-org-card-tone.ts`](src/lib/onboarding/select-org-card-tone.ts) — `selectOrgCardToneFromOnboardingState` / `selectOrgCardToneFromAccountSummary` (aligned with [`resolve-account-entry.ts`](src/lib/onboarding/resolve-account-entry.ts)); tests [`select-org-card-tone.test.ts`](src/lib/onboarding/select-org-card-tone.test.ts).
- **UI:** [`select-organisation-content.tsx`](<src/app/(members)/select-organisation/select-organisation-content.tsx>) — `useQueries` prefetch per account (`GET …/onboarding-state`, same cache as click); card `tone` + merged `isSetup` from onboarding when loaded; simulator branch uses summary flags only.
- **Types / dev:** [`account.ts`](src/types/api/account.ts) optional `hasCompletedOnboardingWizard` on `AccountSummary`; [`select-organisation-sim.ts`](src/lib/dev/select-organisation-sim.ts) `multiple` fixture shows default / warning / error.
- **Grid:** [`grid-card.tsx`](src/components/ui/grid-card.tsx) — `GridCardVisualSlot` org logo `onError` → initials (reset when `imageSrc` changes).

### Decisions

- (none — UI + prefetch pattern; optional CMS bootstrap field documented on `AccountSummary`.)

### Results

- Gateway picker surfaces lifecycle at a glance; parallel onboarding-state queries warm cache for org selection; dev `?orgSim=multiple` exercises three tones without API calls.

### Next

- [ ] Optional: manual spot-check `/select-organisation` in staging (multi-account lifecycle mix) and `?orgSim=multiple` with simulator enabled.

---

## Addendum — `LOG:` Onboarding routing — wizard completes to dashboard (structure review)

### Context

- Implement [`.comms/CODEX/ONBOARDING_STRUCTURE_REVIEW_SETUP_REDIRECT.md`](.comms/CODEX/ONBOARDING_STRUCTURE_REVIEW_SETUP_REDIRECT.md): **`isSetup`** is not a dashboard gate; background sync is a status signal; optional setup route is recovery-only.

### Work Done

- **[`resolve-account-entry.ts`](src/lib/onboarding/resolve-account-entry.ts):** `AccountEntryIntent` → **`dashboard` \| `wizard`** only; wizard completion unlocks scoped routes; removed **`accountEntryIntentAfterWizardConfirm`**.
- **Consumers:** [`create-organisation-wizard.tsx`](<src/app/(members)/create-organisation/_components/create-organisation-wizard.tsx>) — post-W4 → **`accountScopedRoutes.dashboard`**; [`setup-client.tsx`](<src/app/(members)/create-organisation/setup/setup-client.tsx>) — copy/comments for optional route; [`org-access-boundary.tsx`](src/components/auth/org-access-boundary.tsx) — doc comment.
- **Shell:** [`scoped-onboarding-sync-banner.tsx`](src/components/scoped-onboarding-sync-banner.tsx) + [`site-header.tsx`](src/components/site-header.tsx) — info/destructive banner when scoped, wizard-complete, **`!isSetup`** or pipeline failed on onboarding-state.
- **Tests:** [`resolve-account-entry.test.ts`](src/lib/onboarding/resolve-account-entry.test.ts), [`epic1-lifecycle-gate.test.ts`](src/lib/onboarding/epic1-lifecycle-gate.test.ts), [`org-access-boundary.test.tsx`](src/components/auth/org-access-boundary.test.tsx), [`create-organisation-wizard.test.tsx`](<src/app/(members)/create-organisation/_components/create-organisation-wizard.test.tsx>), [`setup-client.test.tsx`](<src/app/(members)/create-organisation/setup/setup-client.test.tsx>).
- **Memory:** [`.memory/decisions.md`](.memory/decisions.md) — **2026-04-09** entry (supersedes **`isSetup`**-only routing for scoped entry).

### Decisions

- (Recorded in **decisions.md** — see **2026-04-09 — Onboarding lifecycle: dashboard when wizard complete**.)

### Results

- Full Vitest suite passed after change; preparation is no longer the default post-wizard destination; sync/failure feedback moves to shell banner + existing setup-status surfaces.

### Next

- [ ] Epic 7 manual QA: spot-check **7.1** flows (wizard → dashboard, scoped entry) against updated resolver expectations.
