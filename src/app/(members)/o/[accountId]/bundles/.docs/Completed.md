# Completed Tickets

### TKT-BUNDLES-001

Restructured `/o/[accountId]/bundles` to match member route conventions: thin pages with metadata, `BundlesScreen` + `useBundlesScreen` (scheduler bootstrap), placeholder panels, development-gated Phase 5–8 API dump, stub `[renderId]` detail route, and feature `.docs`/`.comms`.

### TKT-BUNDLES-002

Wired bundles index (Phase 5 scheduler card, Phase 7 render table with pagination) and detail route (Phase 8 summary, counts, external download links). Dev dump omits Phases 5 and 8 where production UI exists.
