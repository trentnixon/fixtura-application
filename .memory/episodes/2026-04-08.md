# Episode — 2026-04-08

## Context

- **Phase 7 (create-organisation):** Confirm completion logging and product/CMS boundaries after shipping post-**W4** navigation to the scoped dashboard.
- User asked whether **CMS consultation** is required to close Phase 7 or if it is fully complete.
- **Kitchen sink (`/sandbox/kitchen-sink/inputs`):** Log implementation of onboarding-style autocomplete examples (standalone + dependent idle → loading → combobox) for reuse in create-organisation flows.

## Work Done

- **Logged:** Phase 7 app delivery is **complete** for the scoped plan: `router.replace` to `accountScopedRoutes.dashboard(accountId)` after successful W4 in [`create-organisation-wizard.tsx`](<src/app/(members)/create-organisation/_components/create-organisation-wizard.tsx>); feature docs and **TKT-2026-ONB-008** closed (see [`PhasedIntegrationPath.md`](<src/app/(members)/create-organisation/.docs/PhasedIntegrationPath.md>)).
- **Clarified:** No CMS ticket is required for the **redirect** itself (`useConfirmOnboarding` already invalidates `GET /api/account/me`). Optional future alignment: **§5** allowlist before middleware hard-gating; gateway **Q1** (product).
- **Card / Surface elevation:** Default **`Card`** ([`card.tsx`](src/components/ui/card.tsx)) and **`Surface`** ([`container.tsx`](src/components/ui/container.tsx)) use **`border-none shadow-xl ring-1 ring-border`** (replacing `border shadow-sm`), matching kitchen-sink Complex Composition. **`ring-0`** (or explicit `border`) added where callers draw their own edge: [`feedback-card.tsx`](src/components/ui/feedback-card.tsx) variants, [`grid-card-exploration.tsx`](src/app/sandbox/kitchen-sink/cards/grid-card-exploration.tsx) showcase `Surface`, [`contextual.tsx`](src/app/sandbox/kitchen-sink/buttons/_sections/contextual.tsx) dashed empty state, [`containers/page.tsx`](src/app/sandbox/kitchen-sink/containers/page.tsx) themed `Surface`, [`admin/system/page.tsx`](<src/app/(members)/admin/system/page.tsx>) hub cards (hover uses `ring-primary/50`). Kitchen-sink [`cards/page.tsx`](src/app/sandbox/kitchen-sink/cards/page.tsx): Complex Composition simplified; Premium sample uses `ring-primary/20`. [`image-crop/page.tsx`](src/app/sandbox/interaction-lab/upload/image-crop/page.tsx) Card class list trimmed. [`card.test.tsx`](src/components/ui/card.test.tsx) updated; Vitest passes.
- **Interaction lab — colour picker (`/sandbox/interaction-lab/color-picker`):** Shipped **dual** primary/secondary flow per PDR: two **`BrandColorField`**s, **`FixturaAssetColorPreview`** (4:5 gradient mock), **`BrandColorObjectDialog`** for CMS-style JSON; **`lib/brand-color`** pair + dark-on-brand helpers and tests; **`requiredErrorMessage`** on fields. **Popover:** removed preset/recent swatches; fixed overflow (`w-full min-w-0`, `PopoverContent` width). **Layout:** main block uses **`Surface`** (`p-0`, `shadow-sm`, header + body) instead of Card; **`FixturaAssetColorPreview`** uses the same Surface shell. **Warnings:** similarity / both very light / both very dark stay **left**; **white/dark text on gradient** messages render **under the asset preview**.
- **Kitchen sink — inputs (`/sandbox/kitchen-sink/inputs`):** Added **`OnboardingAutocompleteSection`** in [`inputs/_sections/onboarding-autocomplete.tsx`](src/app/sandbox/kitchen-sink/inputs/_sections/onboarding-autocomplete.tsx): (1) standalone searchable combobox with static association labels; (2) sport → club flow with second field **idle** (“Select a sport first”) → **loading** (~850ms, `Loader2`, `aria-busy`) → **ready** combobox from `CLUBS_BY_SPORT` map. Shared **`SearchableCombobox`** (Popover + Command + `h-11` / `rounded-xl`). Wired from [`inputs/page.tsx`](src/app/sandbox/kitchen-sink/inputs/page.tsx). ESLint clean on touched files.

## Decisions

- (none — operational clarification only for Phase 7 / CMS.)
- **Default Card/Surface styling** recorded in [`decisions.md`](../decisions.md) (2026-04-08 — ring + shadow-xl).

## Results

- Phase 7 can be treated as **done** on the app side without blocking on CMS for redirect behaviour. Strapi upstream for earlier phases (A1, W4, S1, etc.) remains separate for live E2E.
- App-wide card and surface chrome is consistent (ring + stronger shadow); call sites with explicit `border`/`shadow-sm` overrides keep their chosen look (e.g. fetch-health).
- Colour picker sandbox is **feature-complete** for the dual-brand PDR (validation split, preview, dialog); production onboarding integration remains out of scope for this lab route.
- Inputs kitchen-sink documents the onboarding combobox pattern (including async second field) without coupling to live APIs.

## Next

- [ ] **QA:** Full onboarding E2E vs Strapi (permissions, W4, redirect, setup-status).
- [ ] **Product/CMS:** **§5** semantics for optional middleware gating; gateway **Q1** when prioritised.
- [ ] Optional: manual pass on **`/sandbox/interaction-lab/color-picker`** (dual fields, dialog JSON, warning placement).
- [ ] Optional: smoke **`/sandbox/kitchen-sink/inputs`** (association combobox; sport → loading → club).

## Links

- [`PhasedIntegrationPath.md`](<src/app/(members)/create-organisation/.docs/PhasedIntegrationPath.md>) (Phase 7)
- [`DevelopmentRoadMap.md`](<src/app/(members)/create-organisation/.docs/DevelopmentRoadMap.md>)
- [`card.tsx`](src/components/ui/card.tsx), [`container.tsx`](src/components/ui/container.tsx) (Surface), [`cards/page.tsx`](src/app/sandbox/kitchen-sink/cards/page.tsx) (kitchen-sink reference)
- [`color-picker-sandbox.tsx`](src/app/sandbox/interaction-lab/color-picker/_components/color-picker-sandbox.tsx), [`fixtura-asset-color-preview.tsx`](src/components/brand-color/fixtura-asset-color-preview.tsx), [`brand-color-popover.tsx`](src/components/brand-color/brand-color-popover.tsx)
- [`onboarding-autocomplete.tsx`](src/app/sandbox/kitchen-sink/inputs/_sections/onboarding-autocomplete.tsx), [`inputs/page.tsx`](src/app/sandbox/kitchen-sink/inputs/page.tsx)

---

## Log checkpoint — user `LOG:`

### Context

- User triggered **`LOG:`** to sync [`.memory`](../) with the interaction-lab brand colour workstream (plan: Brand Color Picker — interaction-lab integration; current app state is the **dual** primary/secondary lab).

### Work Done

- **Memory only (this message):** Confirmed repo surface — [`color-picker-sandbox.tsx`](src/app/sandbox/interaction-lab/color-picker/_components/color-picker-sandbox.tsx) uses two **`BrandColorField`**s, **`FixturaAssetColorPreview`**, **`BrandColorObjectDialog`** (JSON), **`Surface`** shell, pair/similarity/light/dark warnings (left column; gradient readability under preview); **`lib/brand-color`** hex/normalize/contrast + pair helpers; Vitest on hex/contrast utilities; primary feedback stays **inline** (no Sonner on this route).

### Decisions

- (none in this log entry.)

### Results

- [`state.md`](../state.md) refreshed for this checkpoint.

### Next

- [ ] Optional: manual smoke **`/sandbox/interaction-lab/color-picker`** (dual fields, dialog JSON, warning placement).

### Links

- Plan reference (unchanged): [Brand Color Picker Sandbox PDR](src/app/sandbox/interaction-lab/color-picker/.comms/Brand%20Color%20Picker%20Sandbox%20PDR.md)

---

## Log checkpoint — user `LOG:` (create-organisation Getting Started)

### Context

- User requested **`.memory`** sync for the **Getting Started** onboarding workstream: sport selection UX, layout, A1 payload, and temporary **coming soon** sports.

### Work Done

- **`create-organisation-wizard.tsx`:** Step 0 uses **`TypographyPageTitle` / `TypographyPageDescription`** (“Set up”), **full width** (`max-w-none`); sports from **`useOnboardingLookupSports`**, sorted by **`sortOrder`**, **`GridCard`** + **`GridCardVisualSlot`** (`org` initials); **`selectedSportId`** gates **Get started**; **A1** sends **`{ sport }`** when **`needsFirstAccount`**; **`WizardStepOrganisation`** receives **`sportId`**. Sports **not** wrapped in **`Card`**; grid **`justify-center`**; primary row: **`Button` `destructive`** + **`Link`** to **`ROUTES.selectOrganisation`** (“Back to selection”) left, **Get started** right; bottom **Get help** removed (outline **Back to selection** only on steps 1–4).
- **`wizard-step-organisation.tsx`:** **`sportId`** prop; sport **`Select`** removed; effective sport = prop or settings fallback; W1 unchanged.
- **Coming soon (temporary):** **`COMING_SOON_SPORT_LABELS`** — AFL, Hockey, Netball, Basketball (case-insensitive label match); tiles **`tone="mute"`**, **`ctaLabel` “Coming soon”**, **`disabled`**; **`sportChosen`** excludes these labels.
- **`GridCard`** ([`grid-card.tsx`](src/components/ui/grid-card.tsx)):** optional **`disabled`** on non-link tiles (native **`button` `disabled`\*\*, opacity/cursor merge).
- **Comms / docs:** [`.comms/phase-1/app-handoff-post-account-first-endpoint.md`](<src/app/(members)/create-organisation/.comms/phase-1/app-handoff-post-account-first-endpoint.md>) optional **`sport`** body; [`.docs/readMe.md`](<src/app/(members)/create-organisation/.docs/readMe.md>) one-line updates.

### Decisions

- (none logged as durable — coming soon list is a temporary product gate until CMS/API flags exist.)

### Results

- Getting Started is **sport-first**, visually aligned with **grid-card** org tiles; Strapi can adopt **A1** **`sport`** when ready; label-based coming soon is easy to swap for server-driven flags later.

### Next

- [ ] Optional: smoke **`/create-organisation`** (select available sport → A1 / step 1; confirm coming soon tiles non-interactive).
- [ ] **QA:** Full onboarding E2E vs Strapi (unchanged).

### Links

- [`create-organisation-wizard.tsx`](<src/app/(members)/create-organisation/_components/create-organisation-wizard.tsx>), [`wizard-step-organisation.tsx`](<src/app/(members)/create-organisation/_components/wizard-step-organisation.tsx>), [`grid-card.tsx`](src/components/ui/grid-card.tsx)
- [`app-handoff-post-account-first-endpoint.md`](<src/app/(members)/create-organisation/.comms/phase-1/app-handoff-post-account-first-endpoint.md>)

---

## Log checkpoint — user `LOG:` (create-organisation step 1 + CTAs)

### Context

- User requested **`.memory`** sync after **step 1 (Organisation and permission)** UX work: full-width shell, sectioned layout, org-type tiles, association/club comboboxes, permission controls, validation feedback, footer actions, and removal of inline setup status on step 1.

### Work Done

- **`wizard-step-organisation.tsx`:** Step 1 content uses **three `OnboardingSection` surfaces** (organisation type; association/club; permission). **Organisation type** = **`GridCard`** tiles from L2 org types (not Select), **`GridCardIcon`** — **`UsersRound`** vs **`Building2`** by **`CLUB_ACCOUNT_TYPE_ID`**. **Association / club** = shared **`SearchableCombobox`** + **`DependentFieldTrigger`** from [`searchable-combobox.tsx`](src/components/ui/searchable-combobox.tsx) (kitchen-sink pattern); real **`useOnboardingLookupAssociations`** / **`useOnboardingLookupClubs`** data. **Permission** = **`Switch`** (not checkbox). **W1 validation + API catch** errors use **Sonner `toast.error`** (no **`validationError` InlineAlert**). **`SetupStatusCard`** removed from step 1 only (still on review).
- **`create-organisation-wizard.tsx`:** **`max-w-none`** for Get started + organisation step; step 1 **no outer Card** (page title + body + footer). **Footer row:** **`Back`** **`destructive`**, **`Back to selection`** **`Link`** + **`variant="accent"`** (orange/brand), primary **Next / Get started / Finish** on the right; **duplicate bottom “Back to selection”** block removed. Card steps use the same three-action row; review fine print **below** the row.
- **`searchable-combobox.tsx`:** Extracted shared **`SearchableCombobox`**, **`DependentFieldTrigger`** / **`DependentSecondTrigger`**, optional **`loadingMessage`**. Kitchen-sink [`onboarding-autocomplete.tsx`](src/app/sandbox/kitchen-sink/inputs/_sections/onboarding-autocomplete.tsx) imports shared module.

### Decisions

- (none — UI/UX only.)

### Results

- Step 1 matches **sport** + **kitchen-sink** patterns; footers are consistent; validation is non-blocking toast; S1 polling card no longer clutters step 1.

### Next

- [ ] Optional: smoke **`/create-organisation`** (step 1 comboboxes, switches, toasts, footer colours).
- [ ] **QA:** Full onboarding E2E vs Strapi (unchanged).

### Links

- [`wizard-step-organisation.tsx`](<src/app/(members)/create-organisation/_components/wizard-step-organisation.tsx>), [`create-organisation-wizard.tsx`](<src/app/(members)/create-organisation/_components/create-organisation-wizard.tsx>), [`searchable-combobox.tsx`](src/components/ui/searchable-combobox.tsx), [`setup-status-card.tsx`](<src/app/(members)/create-organisation/_components/setup-status-card.tsx>) (review only)

---

## Log checkpoint — user `LOG:` (A1 `hasCompletedStartSequence` + handoff)

### Context

- User requested **`.memory`** sync after shipping **A1** request body **`hasCompletedStartSequence: true`** alongside **`sport`** on Get Started (zero-account path), plus consolidation of the phase-1 handoff doc.

### Work Done

- **App:** [`create-organisation-wizard.tsx`](<src/app/(members)/create-organisation/_components/create-organisation-wizard.tsx>) — **`createFirst.mutate`** sends **`{ sport, hasCompletedStartSequence: true }`** when **`needsFirstAccount`**. **`CreateFirstAccountRequestBody`** in [`account.ts`](src/types/api/account.ts); **`accountApi.createFirstAccount`** / **`useCreateFirstAccount`** typed to that interface.
- **Comms:** [`app-handoff-post-account-first-endpoint.md`](<src/app/(members)/create-organisation/.comms/phase-1/app-handoff-post-account-first-endpoint.md>) — CMS persistence wording for **`Sport`** / **`hasCompletedStartSequence`**; **400** examples; **idempotency** (v1 body not applied on **200**); CMS table + open questions aligned; duplicate **`app-handoff-post-account-first-endpoint copy.md`** removed.

### Decisions

- (none — extends existing A1 contract; CMS must persist when ready.)

### Results

- Strapi **`POST /api/account/first`** can receive **`hasCompletedStartSequence`** with **`sport`**; single canonical handoff doc for A1.

### Next

- [ ] **QA:** Verify Strapi **`createFirstAccount`** persists **`hasCompletedStartSequence`** (and **`sport`**) on create; idempotent **200** behaviour per handoff.
- [ ] **Product/CMS:** **§5** semantics; gateway **Q1** (unchanged).

### Links

- [`app-handoff-post-account-first-endpoint.md`](<src/app/(members)/create-organisation/.comms/phase-1/app-handoff-post-account-first-endpoint.md>), [`account.ts`](src/types/api/account.ts) (`CreateFirstAccountRequestBody`)

---

## Log checkpoint — user `LOG:` (L3 GET themes: `sport` + `theme` JSON)

### Context

- **Contract:** Authenticated **`GET /api/account/onboarding/lookups/themes`** keeps **`{ data: [...] }`**; each theme row adds **`sport`** (account sport enum or null) and **`theme`** (full Theme JSON from CMS or null). Existing **`id`**, **`label`**, **`sortOrder`**, **`slug`** unchanged.
- **Session work:** Types + branding step consume **`theme`** for swatches; endpoint location documented for the team.

### Work Done

- **Types:** [`src/types/api/account.ts`](src/types/api/account.ts) — **`AccountSportEnum`** (`Cricket` | `AFL` | `Hockey` | `Netball` | `Basketball`); **`OnboardingThemeOption`** adds required **`sport`**, **`theme`**; legacy **`primaryColor`/`secondaryColor`** optional.
- **BFF:** [`src/app/api/account/onboarding/lookups/themes/route.ts`](src/app/api/account/onboarding/lookups/themes/route.ts) — JSDoc describes per-row fields (proxy to Strapi unchanged).
- **Branding step:** [`src/app/(members)/create-organisation/_components/wizard-step-branding.tsx`](<src/app/(members)/create-organisation/_components/wizard-step-branding.tsx>) — offline fallbacks set **`sport: null`**, **`theme: null`**; **`resolveThemeRowColours`** prefers CMS **`theme`** via **`themeColoursFromAccountBrandingTheme`**, then **`primaryColor`/`secondaryColor`**, then local swatch fallbacks.

### Decisions

- (none — API DTO alignment and UI consumption only.)

### Results

- Clients can **filter** premade themes by **`sport`** and apply **styling** from **`theme`** without a second fetch; **`tsc`** clean.

### Next

- [ ] Optional: filter Step 2 theme list by account/user **sport** when product prioritises it.

### Links

- [`account.ts`](src/types/api/account.ts) (`AccountSportEnum`, `OnboardingThemeOption`)
- [`wizard-step-branding.tsx`](<src/app/(members)/create-organisation/_components/wizard-step-branding.tsx>), [`theme-colours-from-account.ts`](src/lib/branding/theme-colours-from-account.ts)

---

## Log checkpoint — user `LOG:` (Step 2 theme-only: default vs custom + review swatches)

### Context

- Shipped the **Theme and colours** plan: premade themes as a **visual swatch list**, **custom** inline (**theme name** + **`BrandColorField`**), colours stored on the **theme** in CMS only — **W2** sends **`themeId`** (+ **logo**), not account-level onboarding colour fields; review swatches read from **`branding.theme`** JSON via a shared helper.

### Work Done

- **Types:** [`account.ts`](src/types/api/account.ts) — **`OnboardingThemeOption`** optional **`primaryColor`/`secondaryColor`** (L3); **`AccountBrandingData`** / **`UpdateOnboardingStep2Body`** no longer carry standalone onboarding colour fields on the account; **`AccountBrandingTheme`** optional **`isPublic`**.
- **Helper:** [`theme-colours-from-account.ts`](src/lib/branding/theme-colours-from-account.ts) — **`themeColoursFromAccountBrandingTheme`** reads primary/secondary hex from **`theme.theme`** (bracket-safe field names).
- **Branding step:** [`wizard-step-branding.tsx`](<src/app/(members)/create-organisation/_components/wizard-step-branding.tsx>) — **`brandingMode`** **`default` | `custom`**; default = accessible row list with swatches + **`FixturaAssetColorPreview`** from row hex; custom = inline name + **`BrandColorField`** + **`createTheme`** then W2 **`themeId`**; hydration from catalogue vs private theme; removed Dialog/Select + account colour PATCH; **`useQuery<OnboardingLookupsThemesResponse>`** in [`useOnboardingLookupThemes.ts`](src/lib/api/hooks/account/useOnboardingLookupThemes.ts).
- **Review:** [`wizard-step-review.tsx`](<src/app/(members)/create-organisation/_components/wizard-step-review.tsx>) — swatches from **`themeColoursFromAccountBrandingTheme(brandingPayload?.theme)`** instead of removed account fields.
- **Comms:** [`theme-wizard-theme-only-colours.md`](<src/app/(members)/create-organisation/.comms/theme-wizard-theme-only-colours.md>) — app/CMS handoff for W2 theme-only + GET branding.

### Decisions

- Durable product/contract: see [`decisions.md`](../decisions.md) (2026-04-08 — onboarding Step 2 colours on theme only).

### Results

- **`tsc --noEmit`** passes; ESLint clean on touched branding/theme/L3 hook files. Step 2 aligns with CMS removing account onboarding colour fields when Strapi ships.

### Next

- [ ] **CMS:** Remove deprecated account **`onboardingPrimaryColour`** fields when ready; confirm L3 hex + GET **`theme.theme`** shape in production.
- [ ] Optional: smoke **`/create-organisation`** Step 2 (default row select + custom theme create) and Review swatches.

### Links

- Plan reference (user attachment): theme default vs custom, theme-only storage (not edited in repo).
- [`theme-wizard-theme-only-colours.md`](<src/app/(members)/create-organisation/.comms/theme-wizard-theme-only-colours.md>)

---

## Log checkpoint — user `LOG:` (onboarding footer: centered CTAs + confirm dialogs)

### Context

- User shipped the **onboarding footer** plan: group all bottom actions in one **center-aligned** row; **Back** and **Back to selection** open a **confirmation dialog** before `goBack()` or `router.push(ROUTES.selectOrganisation)` (replacing immediate `Link` navigation).

### Work Done

- **`create-organisation-wizard.tsx`:** **`pendingNav`** state (`'back' | 'selection' | null`); shared **`Dialog`** from `@/components/ui/dialog` (`showCloseButton={false}`); **Cancel** / overlay / **Esc** clears state; **Confirm** runs navigation then clears. Footers use **`flex w-full flex-wrap items-center justify-center gap-3`** for Get started, steps 1–2, and Card steps 3–4 (Back, Back to selection, Next/Finish/Get started in one group). Copy variants: **Leave setup?** for selection (**destructive** confirm **Leave**); **Go back?** with sport-selection wording when **`wizardStepNumber === 1`**, else generic previous step. Removed **`next/link`** for these exits.

### Decisions

- (none — UX pattern only.)

### Results

- Accidental navigation away from onboarding is gated by explicit confirmation; layout matches the centered CTA spec. Vitest suite passed when last run after implementation.

### Next

- [ ] Optional: smoke **`/create-organisation`** (centered footers; Back / Back to selection dialogs; Esc dismisses without navigating).

### Links

- [`create-organisation-wizard.tsx`](<src/app/(members)/create-organisation/_components/create-organisation-wizard.tsx>)
- [`dialog.tsx`](src/components/ui/dialog.tsx)

---

## Log checkpoint — user `LOG:` (Step 2 custom theme: auto name from user + organisation)

### Context

- Shipped the **auto-generate custom theme name** change: onboarding **Custom theme** no longer asks for a **Theme name** input; **`POST …/onboarding/step-2/theme`** **`name`** is built from the signed-in user and current account organisation (same sources as the rest of the wizard).

### Work Done

- **Helper:** [`build-custom-theme-name.ts`](src/lib/onboarding/build-custom-theme-name.ts) — **`buildOnboardingCustomThemeName`** returns **`{ name, isComplete }`**; format **`"{userPart} — {orgPart}"`** (max 255); user from account row names / **`username`** / email local-part; org from **`onboardingOrganisationName`** or **`organisationDetailsFromAccountRow`**. Vitest: [`build-custom-theme-name.test.ts`](src/lib/onboarding/build-custom-theme-name.test.ts).
- **Branding step:** [`wizard-step-branding.tsx`](<src/app/(members)/create-organisation/_components/wizard-step-branding.tsx>) — **`useAccountMe`** + **`activeAccountSummaryFromMePayload`**; removed theme name **`Input`** / state; read-only **Saved as…** copy; submit validates me load + **`isComplete`**; **`customSnapshotRef`** / **unchanged** skip **`createTheme`** by **colours + `themeId`** only (not name).

### Decisions

- Durable: see [`decisions.md`](../decisions.md) (2026-04-08 — custom theme **`name`** system-derived).

### Results

- Custom private themes get consistent, non-editable labels tied to profile + org; **`tsc`** / Vitest / ESLint were clean on the implementation pass.

### Next

- [ ] Optional: smoke **`/create-organisation`** Step 2 **Custom theme** — network **`name`** matches **Saved as** line; unchanged colours skip duplicate **POST theme**.

### Links

- [`build-custom-theme-name.ts`](src/lib/onboarding/build-custom-theme-name.ts), [`wizard-step-branding.tsx`](<src/app/(members)/create-organisation/_components/wizard-step-branding.tsx>)
- [`decisions.md`](../decisions.md) (custom theme name)

---

## Log checkpoint — user `LOG:` (memory sync)

### Context

- User triggered **`LOG:`** again without a new workstream or implementation summary in the message.

### Work Done

- **Memory only:** Confirmed the latest substantive episode entry remains **Step 2 custom theme: auto name from user + organisation** (see checkpoint above). **`state.md`** snapshot refreshed for this **`LOG:`** invocation.

### Decisions

- (none.)

### Results

- No additional repo work recorded in this message; focus/next actions in [`state.md`](../state.md) stay aligned with prior checkpoints.

### Next

- [ ] (unchanged — see [`state.md`](../state.md) _Next actions_.)

### Links

- (none.)

---

## Log checkpoint — user `LOG:` (theme JSON onboarding contract)

### Context

- User requested **`.memory`** sync for the **theme JSON shape and onboarding colour cleanup**: canonical **`theme.theme`** / L3 **`theme`** object **`{ primary, secondary, dark: "#111", white: "#FFF" }`**, **`POST …/onboarding/step-2/theme`** body fields **`primary` / `secondary` / `dark` / `white`**, removal of deprecated account-level colour mentions in docs and of L3 top-level **`primaryColor` / `secondaryColor`** on **`OnboardingThemeOption`**.

### Work Done

- **Parser:** [`theme-colours-from-account.ts`](src/lib/branding/theme-colours-from-account.ts) — exports **`THEME_JSON_DEFAULT_DARK`** / **`THEME_JSON_DEFAULT_WHITE`**, return type includes **`dark`** / **`white`**; parses **`primary` / `secondary`** (legacy **`PrimaryColour` / `SecondaryColour`** only). Vitest: [`theme-colours-from-account.test.ts`](src/lib/branding/theme-colours-from-account.test.ts).
- **Types / wizard:** [`account.ts`](src/types/api/account.ts) — **`CreateOnboardingStep2ThemeBody`** and L3 row shape; [`wizard-step-branding.tsx`](<src/app/(members)/create-organisation/_components/wizard-step-branding.tsx>) — fallback premade **`theme`** objects, **`createTheme`** POST payload.
- **BFF comment:** [`themes/route.ts`](src/app/api/account/onboarding/lookups/themes/route.ts) L3 response note.
- **Docs / comms:** [`theme-wizard-theme-only-colours.md`](<src/app/(members)/create-organisation/.comms/theme-wizard-theme-only-colours.md>), [`PhasedIntegrationPath.md`](<src/app/(members)/create-organisation/.docs/PhasedIntegrationPath.md>), [`cms-handoff-onboarding-phase3-step2.md`](<src/app/(members)/create-organisation/.comms/phase-3/cms-handoff-onboarding-phase3-step2.md>), [`integration-notes-phase3-step2.md`](<src/app/(members)/create-organisation/.comms/phase-3/integration-notes-phase3-step2.md>), plus aligned handoff/request/response/readMe under **create-organisation**.

### Decisions

- Recorded in [`decisions.md`](../decisions.md) — **Theme JSON canonical keys** (see new entry).

### Results

- App and Strapi contract are aligned in code + handoffs; **Strapi must** accept the new custom-theme POST field names and persist/return the four-key **`Theme`** JSON for live Step 2 custom theme + L3 catalogue.

### Next

- [ ] **CMS:** Confirm **`POST …/onboarding/step-2/theme`** and **`GET …/branding`** / L3 **`theme`** match the four-key shape in the target environment.
- [ ] Optional: smoke **Step 2 Custom theme** — network body matches **`primary` / `secondary` / `dark` / `white`**.

### Links

- [`theme-colours-from-account.ts`](src/lib/branding/theme-colours-from-account.ts), [`wizard-step-branding.tsx`](<src/app/(members)/create-organisation/_components/wizard-step-branding.tsx>)
- [`decisions.md`](../decisions.md) (Theme JSON canonical keys)

---

## Log checkpoint — user `LOG:` (create-organisation Step 2 branding UX polish)

### Context

- User triggered **`LOG:`** to record **Branding** onboarding step work: instructional copy, **Logo** / **Theme and colours** sections, **`BrandColorField`** + **`ImageUploaderCrop`** + **`FixturaAssetColorPreview`** behaviour, and hiding the custom **theme name** block while keeping CMS **`name`** on submit.

### Work Done

- **`create-organisation-wizard.tsx`:** Step 2 **`TypographyPageDescription`** — _Add your logo and choose the colours that represent your brand._
- **`wizard-step-branding.tsx`:** Section copy (logo upload, preset vs custom intro, colour field labels/descriptions); removed visible **Theme name** / **Give your theme a name…** / **Saved as…** block; **`buildOnboardingCustomThemeName`** + **`createTheme`** unchanged for **`name`**.
- **`brand-color-field.tsx`:** removed **Choose colour** **Button**; swatch **`PopoverTrigger`** is the only opener; **`cursor-pointer`** when enabled.
- **`image-uploader-crop.tsx`:** **`hasCroppedResult`** gates **`showDropzone`** so dashed area does not show after crop; **Clear** / **Replace** / **`reset`** restores dropzone.
- **`fixtura-asset-color-preview.tsx`:** Logo centred **`w-4/5`** above score row; removed bordered wrapper around logo image.

### Decisions

- (none new — UX/copy only.)

### Results

- Step 2 reads as product copy, not dev/debug; theme **name** remains server-derived without exposing it in the custom theme UI.

### Next

- [ ] Optional: smoke **`/create-organisation`** Step 2 (copy, swatch-only picker, logo crop + dropzone hide/show).

### Links

- [`wizard-step-branding.tsx`](<src/app/(members)/create-organisation/_components/wizard-step-branding.tsx>), [`create-organisation-wizard.tsx`](<src/app/(members)/create-organisation/_components/create-organisation-wizard.tsx>)
- [`brand-color-field.tsx`](src/components/brand-color/brand-color-field.tsx), [`image-uploader-crop.tsx`](src/components/media/image-uploader-crop.tsx), [`fixtura-asset-color-preview.tsx`](src/components/brand-color/fixtura-asset-color-preview.tsx)

---

## Log checkpoint — user `LOG:` (create-organisation Step 3 contact)

### Context

- User requested **`.memory`** sync for **Step 3 (Contact and delivery)** work: layout (centered vs full width), **weekly assets email** semantics on W3 **`deliveryAddress`**, **Send to Me**, client validation + Sonner, removal of the **Sign-in email** section from the step.

### Work Done

- **`create-organisation-wizard.tsx`:** Step 3 uses a **centered** wizard shell (`max-w-2xl` with review), not full width; dedicated branch with page title + **`WizardStepContact`** + footer (Back / Back to selection / Next) — no Card.
- **`wizard-step-contact.tsx`:** **`DeliveryAddress`** treated as **weekly assets email** — **`Input`** `type="email"`, **`isPlausibleEmail`**, **`validateContactStep`** (mandatory first name + email), **`toast.error`** for client and API errors (no submit **`InlineAlert`**). **Send to Me** copies **`useCurrentUser`** email into the field. Removed **Sign-in email** **`OnboardingSection`** and the info alert when sign-in email failed to load.
- **`wizard-step-review.tsx`:** Summary row **Weekly assets email** (replaces postal “delivery address” wording).

### Decisions

- Durable: see [`decisions.md`](../decisions.md) (2026-04-08 — W3 **`deliveryAddress`** = weekly assets email).

### Results

- Step 3 matches product semantics for emailed weekly assets; required-field gating and email shape are enforced client-side before W3 PATCH.

### Next

- [ ] Optional: smoke **`/create-organisation`** Step 3 (required fields, invalid email toast, **Send to Me**).

### Links

- [`wizard-step-contact.tsx`](<src/app/(members)/create-organisation/_components/wizard-step-contact.tsx>), [`create-organisation-wizard.tsx`](<src/app/(members)/create-organisation/_components/create-organisation-wizard.tsx>), [`wizard-step-review.tsx`](<src/app/(members)/create-organisation/_components/wizard-step-review.tsx>)
- [`decisions.md`](../decisions.md) (W3 delivery email)

---

## Log checkpoint — user `LOG:` (create-organisation Step 4 review UX + theme swatches)

### Context

- Shipped **Step 4 review** polish: **no outer `Card`** on the step (parity with steps 2–3), then follow-up to remove **`SetupStatusCard`** from review, **merge footer fine-print** into one sentence in a **`Card`**, and **fix branding swatches** when **`GET …/branding`** theme JSON is empty or uses alternate keys / shorthand hex.

### Work Done

- **`create-organisation-wizard.tsx`:** Step 4 uses **`header` + `TypographyPageTitle` / `TypographyPageDescription`** (`WIZARD_STEPS[3]`) + body + footer; **Finish** footer copy merged into one sentence inside **`Card` + `CardContent`** (pre‑ vs post‑completion variants). Removed **`Card`**-wrapped step chrome (earlier pass) and **`activeWizardDefinition`**.
- **`wizard-step-review.tsx`:** Removed **`SetupStatusCard`** (main + confirmed success); removed duplicate **`TypographyFinePrint`** in the step body; **`useOnboardingLookupThemes`** + **`themeColoursForReviewStep`** for swatches; pending state includes themes lookup; import order ESLint fix.
- **`theme-colours-from-account.ts`:** **`parsePrimaryFromThemeJson` / `parseSecondaryFromThemeJson`** (alternate keys); **`accountThemeJsonHasExplicitPrimarySecondary`**; **`themeColoursForReviewStep`** (API vs catalogue by **`theme.id`**).
- **`hex.ts`:** **`tryNormalizeHex`** expands **`#RGB`** shorthand.
- **Tests:** [`theme-colours-from-account.test.ts`](src/lib/branding/theme-colours-from-account.test.ts), [`hex.test.ts`](src/lib/brand-color/hex.test.ts).

### Decisions

- Durable: see [`decisions.md`](../decisions.md) (2026-04-08 — Step 4 review: no **`SetupStatusCard`**, merged footer copy, **`themeColoursForReviewStep`**).

### Results

- Review matches the **OnboardingSection** rhythm; footer copy is single-surface **`Card`**; review swatches align with CMS/L3 when the account theme payload is incomplete.

### Next

- [ ] Optional: smoke **`/create-organisation`** Step 4 (no setup card; **Finish** card copy; swatches for premade + custom themes).
- [ ] (unchanged — see _Next actions_ in [`state.md`](../state.md).)

### Links

- [`create-organisation-wizard.tsx`](<src/app/(members)/create-organisation/_components/create-organisation-wizard.tsx>), [`wizard-step-review.tsx`](<src/app/(members)/create-organisation/_components/wizard-step-review.tsx>)
- [`theme-colours-from-account.ts`](src/lib/branding/theme-colours-from-account.ts), [`hex.ts`](src/lib/brand-color/hex.ts)
- [`decisions.md`](../decisions.md) (Step 4 review)

---

## Log checkpoint — user `LOG:` (onboarding lifecycle v1 + scoped app when wizard complete)

### Context

- Shipped **onboarding lifecycle v1** in the members app (BFF + client) per [`.comms/onBoarding/app-handoff-onboarding-lifecycle-v1-integration.md`](../../.comms/onBoarding/app-handoff-onboarding-lifecycle-v1-integration.md) and Q&A in [`.comms/onBoarding/app-open-questions-onboarding-lifecycle-v1.md`](../../.comms/onBoarding/app-open-questions-onboarding-lifecycle-v1.md).
- Product decided **Q11–Q13:** users may enter **`/o/[accountId]/…`** when **`hasCompletedOnboardingWizard`** is true, even if **`isSetup`** is false (background fetch continues).

### Work Done

- **BFF:** [`onboarding-state/route.ts`](../../src/app/api/accounts/[accountId]/onboarding/onboarding-state/route.ts), [`retry-setup/route.ts`](../../src/app/api/accounts/[accountId]/onboarding/retry-setup/route.ts); [`route-definitions.ts`](../../src/lib/api/routes/route-definitions.ts) entries.
- **Types / parse:** [`account.ts`](../../src/types/api/account.ts) (`OnboardingStateData`, pipeline enums, extended setup-status); [`parse-onboarding-state.ts`](../../src/lib/api/parse-onboarding-state.ts), [`parse-onboarding-setup-status.ts`](../../src/lib/api/parse-onboarding-setup-status.ts).
- **API / hooks:** [`account.api.ts`](../../src/lib/api/services/account.api.ts); [`query-keys.ts`](../../src/lib/api/query/query-keys.ts) `onboardingState`; [`useOnboardingOnboardingState.ts`](../../src/lib/api/hooks/account/useOnboardingOnboardingState.ts), [`useRetryOnboardingSetup.ts`](../../src/lib/api/hooks/account/useRetryOnboardingSetup.ts); invalidations on confirm + step hooks; [`onboarding.ts`](../../src/lib/config/onboarding.ts) terminal **`failed`**.
- **UI:** [`create-organisation-wizard.tsx`](<../../src/app/(members)/create-organisation/_components/create-organisation-wizard.tsx>) — server step hydration; **`canOpenScopedApp`**; async confirm success + **`fetchQuery`**; removed blocking preparation-only page after product change; [`wizard-step-review.tsx`](<../../src/app/(members)/create-organisation/_components/wizard-step-review.tsx>) async **`onConfirmSuccess`**. [`setup-status-card.tsx`](<../../src/app/(members)/create-organisation/_components/setup-status-card.tsx>) **`failed`** copy, pipeline detail, optional **`showRetryOnFailure`**.
- **Tests:** [`parse-onboarding-state.test.ts`](../../src/lib/api/parse-onboarding-state.test.ts), [`onboarding.test.ts`](../../src/lib/config/onboarding.test.ts).

### Decisions

- **Scoped app when wizard complete (even if `!isSetup`):** recorded in [`decisions.md`](../decisions.md) (2026-04-08).

### Results

- **`lint` / `typecheck` / targeted Vitest** passed after implementation. Strapi must expose upstream routes and permissions for live E2E.

### Next

- [ ] **Ops/QA:** Enable **`getOnboardingState`**, **`retryOnboardingSetup`** on Authenticated; E2E onboarding-state + post-W4 redirect + optional **`!isSetup`** dashboard smoke.
- [ ] Optional: **soft banner** on dashboard when **`!isSetup`** and wizard complete.

### Links

- [`.comms/onBoarding/app-handoff-onboarding-lifecycle-v1-integration.md`](../../.comms/onBoarding/app-handoff-onboarding-lifecycle-v1-integration.md)
- [`create-organisation-wizard.tsx`](<../../src/app/(members)/create-organisation/_components/create-organisation-wizard.tsx>)
- [`decisions.md`](../decisions.md) (scoped members when wizard complete)

---

## Log checkpoint — user `LOG:` (`rgbDistance` invalid-input test)

### Context

- Vitest failed: **`rgbDistance("bad", "#79001F")`** expected **`null`** but returned a distance; **`"bad"`** parses as valid 3-digit hex shorthand (**`#BBAADD`**) via **`tryNormalizeHex`** (same rule as bare **`"abc"`** in [`hex.test.ts`](../../src/lib/brand-color/hex.test.ts)).

### Work Done

- **Tests:** [`pair.test.ts`](../../src/lib/brand-color/pair.test.ts) — invalid-input example replaced with **`"f20100g"`** (invalid tail char, consistent with hex tests); one-line comment so **`"bad"`** is not mistaken for invalid hex.

### Decisions

- (none.)

### Results

- **`npx vitest run src/lib/brand-color/pair.test.ts`** passes.

### Next

- [ ] (unchanged — see _Next actions_ in [`state.md`](../state.md).)

### Links

- [`pair.ts`](../../src/lib/brand-color/pair.ts), [`hex.ts`](../../src/lib/brand-color/hex.ts)

---

## Log checkpoint — user `LOG:` (onboarding route recovery)

### Context

- User triggered **`LOG:`** after shipping **onboarding lifecycle route recovery** per plan (gateway resolves **`onboarding-state`** before scoped entry; dashboard only when **`isSetup === true`**; preparation route; scoped boundary enforcement; multi-account **`?accountId=`**).

### Work Done

- **Memory:** New durable decision in [`decisions.md`](../decisions.md) — **account-scoped app only when `isSetup === true`** (supersedes same-day “wizard complete → scoped” routing for navigation). [`state.md`](../state.md) refreshed: current focus + QA next actions aligned with recovery behaviour.
- **Repo reference (implementation session):** [`resolve-account-entry.ts`](../../src/lib/onboarding/resolve-account-entry.ts) + tests; [`create-organisation-wizard.tsx`](<../../src/app/(members)/create-organisation/_components/create-organisation-wizard.tsx>); [`select-organisation-content.tsx`](<../../src/app/(members)/select-organisation/select-organisation-content.tsx>); [`org-access-boundary.tsx`](../../src/components/auth/org-access-boundary.tsx); [`create-organisation/setup/`](<../../src/app/(members)/create-organisation/setup/>); [`ROUTES.createOrganisationSetup`](../../src/lib/config/routes.ts). **Delete account** remains deferred (no API).

### Decisions

- Logged in [`decisions.md`](../decisions.md) — scoped entry gate (**`isSetup`** only).

### Results

- `.memory` matches the route-recovery product rule; historical decision text for “wizard complete → scoped” retained under **Decisions** with explicit supersede note on the new entry.

### Next

- [ ] **QA:** See [`state.md`](../state.md) _Next actions_ (lifecycle gating, preparation, deep links).

### Links

- [`.comms/CODEX/ACCOUNT_ONBOARDING_ROUTE_RECOVERY_PLAN.md`](../../.comms/CODEX/ACCOUNT_ONBOARDING_ROUTE_RECOVERY_PLAN.md)
- [`decisions.md`](../decisions.md), [`state.md`](../state.md)

---

## Log checkpoint — user `LOG:` (Epic 1 lifecycle enforcement plan)

### Context

- User asked to **implement** the **Epic 1** plan (enforce lifecycle before dashboard entry): verify acceptance, simulator behaviour, optional server gate, automated tests, backlog documentation.

### Work Done

- **`accountEntryFromOnboardingState`** in [`resolve-account-entry.ts`](../../src/lib/onboarding/resolve-account-entry.ts) — single helper for **`resolveAccountEntry` + `accountEntryPath`** used by **`select-organisation`** and **`OrgAccessBoundary`**.
- **[`select-organisation-content.tsx`](<../../src/app/(members)/select-organisation/select-organisation-content.tsx>):** `router.push(accountEntryFromOnboardingState(data, accountId))` after **`GET …/onboarding-state`**; comment that dev **`?orgSim=`** ( **`NEXT_PUBLIC_SELECT_ORG_SIMULATOR`**) intentionally skips lifecycle and opens scoped dashboard for layout testing.
- **[`org-access-boundary.tsx`](../../src/components/auth/org-access-boundary.tsx):** redirect uses **`accountEntryFromOnboardingState`** for parity with selection.
- **Tests:** [`epic1-lifecycle-gate.test.ts`](../../src/lib/onboarding/epic1-lifecycle-gate.test.ts) — Epic 1 routing matrix; full Vitest suite passes.
- **Docs:** [`.comms/CODEX/ONBOARDING_IMPLEMENTATION_BACKLOG.md`](../../.comms/CODEX/ONBOARDING_IMPLEMENTATION_BACKLOG.md) — Epic 1 **Status (2026-04-08)**, tickets **1.1** / **1.2** marked **Done**, verification notes, simulator + **server-side gate waived** (client **`OrgAccessBoundary`** only).

### Decisions

- (none new — execution of existing route-recovery policy; server redirect deferred.)

### Results

- Epic 1 backlog items are **documented as complete** with tests guarding the resolver matrix; simulator bypass is explicit in code and backlog.

### Next

- [ ] **QA:** Manual smoke select-org + deep-link **`/o/[accountId]/…`** with unfinished onboarding (unchanged from [`state.md`](../state.md)).
- [ ] **Epic 2+** per onboarding backlog (wizard shortcuts, delete when API exists).

### Links

- [`epic1-lifecycle-gate.test.ts`](../../src/lib/onboarding/epic1-lifecycle-gate.test.ts), [`resolve-account-entry.ts`](../../src/lib/onboarding/resolve-account-entry.ts)
- [`.comms/CODEX/ONBOARDING_IMPLEMENTATION_BACKLOG.md`](../../.comms/CODEX/ONBOARDING_IMPLEMENTATION_BACKLOG.md) (Epic 1)

---

## Log checkpoint — user `LOG:` (Epic 2 wizard completion vs readiness)

### Context

- User requested **`.memory`** sync after shipping **Epic 2** ([`.comms/CODEX/ONBOARDING_IMPLEMENTATION_BACKLOG.md`](../../.comms/CODEX/ONBOARDING_IMPLEMENTATION_BACKLOG.md) — correct wizard completion behaviour: no dashboard until **`isSetup`**, preparation route for completed-but-not-ready accounts, resume **`not_started` / `in_progress`** unchanged).

### Work Done

- **`accountEntryIntentAfterWizardConfirm`** in [`resolve-account-entry.ts`](../../src/lib/onboarding/resolve-account-entry.ts) — after W4 confirm, **`dashboard`** only when **`isSetup === true`**; **`preparation` / `preparationFailed`** when resolver matches; otherwise **`preparation`** (stale refetch still showing **`wizard`**).
- **`create-organisation-wizard.tsx`** — **`handleConfirmSuccess`** always **`router.replace(accountEntryPath(intent, accountId))`** when **`accountId`** is set; footer copy when **`wizardCompleted`** (no-`accountId` edge) no longer implies opening the dashboard.
- **`setup-client.tsx`** — dashboard **`replace`** only from **`onboardingData.isSetup === true`**; removed **`setup-status` `ready`** → dashboard shortcut and matching loader; when status is **`ready`** but **`!isSetup`**, **`invalidateQueries`** **`onboarding-state`** to pick up **`isSetup`**.
- **Tests:** [`resolve-account-entry.test.ts`](../../src/lib/onboarding/resolve-account-entry.test.ts) — **`accountEntryIntentAfterWizardConfirm`** cases.

### Decisions

- Durable: see [`decisions.md`](../decisions.md) (2026-04-08 — Epic 2 post-W4 + preparation routing).

### Results

- Gateway wizard completion no longer collapses to dashboard on **`hasCompletedOnboardingWizard`** alone; preparation stays consistent with **`isSetup`**-only scoped entry.

### Next

- [ ] **QA:** W4 → preparation when **`!isSetup`**; **`setup`** page → dashboard only after **`isSetup`**; stale wizard refetch still lands on **`/create-organisation/setup`**.

### Links

- [`resolve-account-entry.ts`](../../src/lib/onboarding/resolve-account-entry.ts), [`create-organisation-wizard.tsx`](<../../src/app/(members)/create-organisation/_components/create-organisation-wizard.tsx>), [`setup-client.tsx`](<../../src/app/(members)/create-organisation/setup/setup-client.tsx>)
- [`decisions.md`](../decisions.md) (Epic 2 routing)

---

## Log checkpoint — user `LOG:` (Epic 3 setup preparation and recovery)

### Context

- User triggered **`LOG:`** after completing the **Epic 3** plan: gateway **`/create-organisation/setup`** edge auth parity with **`/create-organisation`**, automated tests, backlog status, verification (**`npm test -- --run`**).

### Work Done

- **`middleware.ts`:** Exported **`isMembersGatewayProtectedPath`** — **`/select-organisation`**, exact **`/create-organisation`**, and **`/create-organisation/*`** (e.g. **`/create-organisation/setup`**) require the same cookie gate as other protected members routes. Matcher adds **`/create-organisation/:path*`** so middleware runs on nested paths.
- **Tests:** [`middleware.test.ts`](../../src/middleware.test.ts) — matcher + **`isMembersGatewayProtectedPath`**; [`setup-status-card.test.tsx`](<../../src/app/(members)/create-organisation/_components/setup-status-card.test.tsx>) — retry button visibility and **`mutate`** on click (mocked hooks).
- **Docs:** [`.comms/CODEX/ONBOARDING_IMPLEMENTATION_BACKLOG.md`](../../.comms/CODEX/ONBOARDING_IMPLEMENTATION_BACKLOG.md) — Epic 3 **Status: Completed** + completion note (links to setup page, tests).

### Decisions

- (none — implementation hardening and tests; product rules unchanged from Epic 1 / 2.)

### Results

- Full Vitest suite passed when last run after implementation; preparation route is classified as gateway-protected at the middleware layer (no longer relying only on **`MembersSessionBoundary`** for that classification).

### Next

- [ ] **QA:** Optional smoke **`/create-organisation/setup?accountId=`** unauthenticated → sign-in (middleware); failed setup → **Retry setup** vs Strapi.
- [ ] (unchanged — see [`state.md`](../state.md) _Next actions_ for broader onboarding E2E.)

### Links

- [`middleware.ts`](../../src/middleware.ts), [`setup-status-card.test.tsx`](<../../src/app/(members)/create-organisation/_components/setup-status-card.test.tsx>)
- [`.comms/CODEX/ONBOARDING_IMPLEMENTATION_BACKLOG.md`](../../.comms/CODEX/ONBOARDING_IMPLEMENTATION_BACKLOG.md) (Epic 3)
