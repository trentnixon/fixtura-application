/**
 * Client-side minimums for logo source and crop output (align with route lab validation scenario).
 * Server limits may differ; tune when CMS documents M1 constraints.
 */
export const LOGO_MIN_SOURCE_WIDTH_PX = 500;
export const LOGO_MIN_SOURCE_HEIGHT_PX = 500;
export const LOGO_MIN_OUTPUT_WIDTH_PX = 400;
export const LOGO_MIN_OUTPUT_HEIGHT_PX = 400;
