import type { ReactNode } from "react";

export const metadata = {
  title: "Image upload and crop — Interaction lab",
  description: "Sandbox for image dropzone, aspect crop, and optional Strapi M1 upload.",
};

export default function ImageCropLabLayout({ children }: { children: ReactNode }) {
  return children;
}
