# Episode — 2026-04-25

## Context

- Build out a new Season section in Data Lab so the team can validate the new `season-hub` read-model routes and quickly iterate on UI design patterns.

## Work Done

- Added a new `Season` section to Data Lab navigation with account `575` sample links.
- Created new Data Lab season routes for overview, canonical drill-down, and alias drill-down flows under `src/app/sandbox/data-lab/season/575/...`.
- Wired pages to existing season hooks (`useSeasonHubRecon`, `useSeasonHubStats`, `useSeasonHubCompetitions`, `useSeasonHubCompetition`, `useSeasonHubCompetitionGrades`, `useSeasonHubGrade`, `useSeasonHubGradeFixtures`, `useSeasonHubFixture`).
- Implemented endpoint contract display, loading/error states, and refetch controls across season pages.
- Iterated on sidebar and section-title styling, then reverted reveal/dropdown behavior back to normal nav per request.
- Refined season page UI to align with Kitchen Sink patterns (`PageHeader`, `Surface Layout`, row-link lists, and stat-card tiles on overview).
- Updated competition page so the `Competition payload` block uses `Surface Layout` and appears at the bottom.

## Decisions

- Standardize Data Lab Season pages on Kitchen Sink visual primitives (Surface Layout containers and stat cards) to keep sandbox UI consistent with application-approved patterns.

## Results

- `/sandbox/data-lab/season/*` now provides complete route coverage for season-hub contract testing with account `575`.
- Season overview metrics now use stat-card style tiles for Competitions, Grades, Teams, and Fixtures.
- Endpoint and payload sections now use consistent Surface-based presentation.
- Lint checks were run on touched files during implementation and remained clean.

## Next

- [ ] Manually validate all new season data-lab routes with live data (`overview`, canonical, alias) in signed-in and signed-out states.
- [ ] Verify canonical vs alias fixture payload parity for known fixture ids and document discrepancies if found.
- [ ] Consider adding active-link highlighting in Data Lab sidebar for easier drill-down navigation.

## Links

- `src/lib/dev-sandbox-nav.ts`
- `src/app/sandbox/data-lab/layout.tsx`
- `src/app/sandbox/data-lab/season/575/_components/season-lab-frame.tsx`
- `src/app/sandbox/data-lab/season/575/overview/page.tsx`
- `src/app/sandbox/data-lab/season/575/competitions/[competitionId]/page.tsx`

# Episode — 2026-04-25

## Context

- Adjust the development debug panel layout/size in response to a quick UI tweak request during active feature work.

## Work Done

- Located the debug panel component in `src/components/dev/dev-debug-panel.tsx`.
- Implemented a horizontal layout pass for the panel sections, then reverted it on request.
- Applied a final size tweak to make the panel thinner by changing width from `w-80` to `w-72`.

## Results

- Dev Debug Panel remains vertically stacked.
- Panel width is reduced and less intrusive on screen.
- Final code state matches the latest user direction ("make it thinner").

## Next

- [ ] Validate the thinner panel in the browser at common viewport sizes.
- [ ] Confirm whether the panel should stay fixed at `w-72` or use responsive width rules.

## Links

- `src/components/dev/dev-debug-panel.tsx`

# Episode — 2026-04-25

## Context

- Build out the new `route-lab` season flow for account `575` using the same data-fetch scope patterns proven in `data-lab`.
- Bring route-lab pages closer to FE user-facing UI while keeping debugging clearly separated.

## Work Done

- Added and wired the fixed-ID season route tree under `src/app/sandbox/route-lab/season/575/*` for overview, canonical competition/grade/fixture, and alias grade/fixture paths.
- Connected each route page to the matching `season-hub` hooks for recon/stats/competitions/competition/grades/fixture fetch scopes.
- Added a shared route-lab frame and helpers for loading/error/list rows and standardized debug display.
- Updated route-lab sidebar navigation to show a single `Season` entry under App, linking to `/sandbox/route-lab/season/575/overview`.
- Refined overview page UX to FE-facing style: simplified header, removed dev summary blocks/resources chips, switched stat tiles to compact metric style, and kept debugging cards at the bottom.
- Updated competition page (`/sandbox/route-lab/season/575/competitions/18031`) to FE-facing structure with compact metrics and a settings-surface style grade links card.
- Replaced inline raw payload sections with explicit debugging payload cards where applicable.
- Ran lint checks on edited route-lab files during the session; no new lint issues were introduced in touched files.

## Decisions

- Keep FE-facing route content free of dev/debug wording in primary headers; place all debug controls and payload visibility in a dedicated bottom debugging section using critical tinted feedback cards.
- Use compact metric card treatment (`card.metric.inline-compact`) and settings-surface list treatment (`card.surface.settings`) for season route-lab FE-style summary and navigation blocks.

## Results

- Route-lab now has a season walkthrough that mirrors data-lab fetch coverage while presenting FE-oriented page structure and styling.
- Debug data remains available but visually isolated from user-facing sections.
- Navigation and CTA usage align with requested app patterns for this feature area.

## Next

- [ ] Apply the same FE-facing alignment pattern across remaining season route-lab detail pages for full visual consistency.
- [ ] Do a manual click-through pass of `/sandbox/route-lab/season/575/*` to validate end-to-end transitions and empty/error states.
- [ ] Run a broader lint/typecheck sweep when convenient.

## Links

- `/sandbox/route-lab/season/575/overview`
- `/sandbox/route-lab/season/575/competitions/18031`

# Episode — 2026-04-25

## Context

- Integrate the new `season-hub` endpoint family into the members Season area with the standard Fixtura API pattern (route registry, BFF, service, hooks, UI).
- Align season access behavior with onboarding/setup states and improve empty-state handling for low/zero-data accounts.

## Work Done

- Added BFF catch-all proxy at `src/app/api/season-hub/[accountId]/[...slug]/route.ts` with cookie-to-Bearer forwarding, account id validation, query passthrough, and preserved upstream JSON error payloads.
- Registered season-hub route metadata in `src/lib/api/routes/route-definitions.ts`.
- Added season-hub API contracts in `src/types/api/season-hub.ts`.
- Added `seasonHubApi` service in `src/lib/api/services/season-hub.api.ts`.
- Extended query keys with a dedicated `seasonHub` namespace in `src/lib/api/query/query-keys.ts`.
- Implemented season hooks under `src/lib/api/hooks/season-hub/*` plus barrel exports.
- Replaced the placeholder season page with hooked overview UI and added competition/grade/fixture drill-down routes and components.
- Added season-focused tests for query keys and competitions hook behavior.
- Added season-specific onboarding lock shell and then updated lock criteria to `state.isSetup !== true`.
- Removed the “Continue setup” CTA from the season lock panel, leaving informational copy only.
- Added reusable `season-empty-panel` and expanded empty handling for zero competitions, zero grades, and zero fixtures across season overview/competition/grade screens.

## Decisions

- Season route lock condition is now based on setup completion (`state.isSetup === true` required), not wizard completion.
- Season lock UX is informational only (no setup CTA in the panel).
- Season pages should provide explicit empty-state messaging for zero-data scenarios at each level (overview, competition grades, grade fixtures).

## Results

- Members Season now uses the repo’s established data-fetch architecture end-to-end.
- Season access is blocked until setup is complete, matching the requested metric.
- Empty/zero states are clearer for accounts with no season resources yet.
- New season query key and hook tests pass.

## Next

- [ ] Run a broader app-level typecheck/lint sweep when convenient.
- [ ] Manually verify season lock behavior for accounts in each onboarding/setup lifecycle combination.
- [ ] Validate production-like payload shape drift for competition/grade detail and tighten loose `Record<string, unknown>` contracts where safe.

## Links

- `src/app/api/season-hub/[accountId]/[...slug]/route.ts`
- `src/lib/api/routes/route-definitions.ts`
- `src/types/api/season-hub.ts`
- `src/lib/api/services/season-hub.api.ts`
- `src/lib/api/query/query-keys.ts`
- `src/lib/api/hooks/season-hub/`
- `src/app/(members)/o/[accountId]/season/`

# Episode — 2026-04-25

## Context

- Refactor `season/_components` into smaller maintainable modules and resolve hook-order lint/runtime safety issues introduced during extraction.

## Work Done

- Audited season components under `src/app/(members)/o/[accountId]/season/_components`.
- Introduced `_types`, `_utils`, `_hooks`, `_constants`, plus `shared/summary-tile.tsx`.
- Moved inline parsing and shaping logic (id/name pickers, headline/title resolvers, count extraction, overview derivations) into dedicated utility and hook files.
- Refactored `season-overview`, `season-competition-detail`, `season-grade-view`, `season-fixture-view`, `season-onboarding-shell`, and `season-empty-panel` to consume extracted modules.
- Fixed hook-order violation by moving `useSeasonCompetitionDetailState` above early returns in `season-competition-detail.tsx`.
- Verified targeted file lint passes after the hook-order fix.

## Decisions

- Standardize this season feature extraction using `_constants`, `_hooks`, `_types`, and `_utils` naming to align with the requested folder contract and keep top-level component files orchestration-focused.

## Results

- Season component files are smaller and responsibilities are split by concern.
- Hook-order error in `season-competition-detail.tsx` is resolved.
- Targeted lint verification for the fixed file is clean.

## Next

- [ ] Apply the same unconditional-hook ordering fix pattern to `season-grade-view.tsx` and `season-overview.tsx` if those files are still reporting hook-order warnings in broader lint runs.
- [ ] Run a focused lint pass on all season `_components` files and resolve remaining import-order warnings.
- [ ] Re-run full project typecheck after unrelated route-definition domain typing issue is addressed.

## Links

- `src/app/(members)/o/[accountId]/season/_components/season-competition-detail.tsx`
- `src/app/(members)/o/[accountId]/season/_components/_hooks/use-season-competition-detail-state.ts`
- `src/app/(members)/o/[accountId]/season/_components/_utils/index.ts`

# Episode — 2026-04-25

## Context

- Confirm whether the members route `/o/573/season` is already receiving season data or is still a routing placeholder.

## Work Done

- Traced route registration and path mapping for the scoped members season URL.
- Reviewed `src/app/(members)/o/[accountId]/season/page.tsx` and confirmed the page content is currently static placeholder UI.
- Verified scoped shell and boundary behavior (`MembersAppShell`, `OrgAccessBoundary`, sidebar/header) to identify indirect data fetching still active on the route.
- Checked account-scoped API surface and confirmed there is no dedicated `season` endpoint under `src/app/api/accounts/[accountId]/...`.

## Results

- `/o/:accountId/season` is wired and reachable via scoped routing/navigation.
- No season-specific payload is loaded on the page yet.
- Only shared scoped context data is fetched (organisation access/onboarding/me/sidebar context).

## Next

- [ ] Define the intended season data contract for members season view (`/api/accounts/[accountId]/season` or existing `season-hub` path).
- [ ] Replace season placeholder page content with hook-driven data UI once contract is confirmed.
- [ ] Run a manual browser check on `/o/573/season` after wiring to validate loading, empty, and error states.

## Links

- `src/app/(members)/o/[accountId]/season/page.tsx`
- `src/components/auth/org-access-boundary.tsx`
- `src/lib/config/account-routes.ts`
