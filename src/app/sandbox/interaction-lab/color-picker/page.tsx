import { ColorPickerSandbox } from "./_components/color-picker-sandbox";

export const metadata = {
  title: "Colour picker — Interaction lab",
  description:
    "Dual brand colours: primary and secondary fields, live 4:5 gradient preview, validation, and dialog with CMS-style JSON (lab only).",
};

export default function InteractionLabColorPickerPage() {
  return <ColorPickerSandbox />;
}
